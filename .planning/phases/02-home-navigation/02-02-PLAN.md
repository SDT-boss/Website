---
phase: 02-home-navigation
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - app/globals.css
  - messages/en.json
  - messages/id.json
  - messages/zh.json
  - components/home/HeroSection.tsx
  - components/home/SolutionsStrip.tsx
  - components/home/SolutionCard.tsx
  - components/home/ApproachSection.tsx
  - components/home/WorkTeaser.tsx
  - components/home/FooterCTA.tsx
  - app/[locale]/page.tsx
autonomous: false
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05

must_haves:
  truths:
    - "Hero section renders with dot-grid background, eyebrow label, 2-line H1, sub-line, and exactly one Volt Green CTA button"
    - "Four solution cards render with icon, title, description, and grid-violet tag; hover shifts border to cyber-jade"
    - "Approach section shows 3 numbered items with text-cyber-jade/20 large number accents"
    - "Work teaser section shows the EV Fleet Operations card with bg-ev-teal/10 text-ev-teal status tag and a locale-prefixed link to /work"
    - "Footer CTA section uses a cyber-jade outline button (border-cyber-jade text-cyber-jade) — NOT volt-green"
    - "All three locale routes (/en, /id, /zh) render translated home page content"
    - "npm run build exits 0 with static pages emitted for /en/, /id/, /zh/"
  artifacts:
    - path: "app/globals.css"
      provides: ".dot-grid CSS class"
      contains: "radial-gradient(circle, #2a2a2a 1px"
    - path: "components/home/HeroSection.tsx"
      provides: "Hero section with dot-grid, eyebrow, 2-line H1, sub, Volt Green CTA"
      contains: "bg-volt-green"
    - path: "components/home/SolutionsStrip.tsx"
      provides: "4-card solutions grid with section label"
      contains: "SolutionCard"
    - path: "components/home/SolutionCard.tsx"
      provides: "Individual card component with typed props"
      contains: "bg-grid-violet/10 text-grid-violet"
    - path: "components/home/ApproachSection.tsx"
      provides: "3-item approach section with numbered accents"
      contains: "text-cyber-jade/20"
    - path: "components/home/WorkTeaser.tsx"
      provides: "Work teaser card + link to /work"
      contains: "bg-ev-teal/10 text-ev-teal"
    - path: "components/home/FooterCTA.tsx"
      provides: "Footer CTA section with cyber-jade outline button"
      contains: "border-cyber-jade text-cyber-jade"
    - path: "app/[locale]/page.tsx"
      provides: "Home page Server Component composing all sections"
      contains: "setRequestLocale"
    - path: "messages/en.json"
      provides: "English translation keys for home, solutions, work, approach namespaces"
      contains: "footerCtaHeading"
    - path: "messages/id.json"
      provides: "Indonesian translations matching en.json structure"
      contains: "footerCtaHeading"
    - path: "messages/zh.json"
      provides: "Chinese Simplified translations matching en.json structure"
      contains: "footerCtaHeading"
  key_links:
    - from: "app/[locale]/page.tsx"
      to: "components/home/HeroSection.tsx"
      via: "import { HeroSection } from @/components/home/HeroSection"
      pattern: "HeroSection.*@/components/home"
    - from: "components/home/HeroSection.tsx"
      to: "messages/en.json"
      via: "getTranslations(home)"
      pattern: "getTranslations.*home"
    - from: "components/home/FooterCTA.tsx"
      to: "config/site.ts"
      via: "siteConfig.email in mailto href"
      pattern: "siteConfig\\.email"
    - from: "components/home/WorkTeaser.tsx"
      to: "i18n/navigation.ts"
      via: "import { Link } from @/i18n/navigation"
      pattern: "Link.*@/i18n/navigation"
---

## Phase Goal

**As a** visitor landing on the SDT home page, **I want to** see the complete home page — hero, solutions, approach, work teaser, and a contact CTA — **so that** I immediately understand what SDT builds, can navigate to learn more, and have a clear path to reach out.

<objective>
Build all five home page sections as Server Components plus expand translation keys across all three locale files. Wire them together in app/[locale]/page.tsx so the home page is fully live at /en, /id, and /zh.

Purpose: This plan delivers the primary conversion surface for the entire site. It depends on PLAN-01 (Wave 1) completing so that i18n/navigation.ts and Navbar.tsx are in place.

Output: .dot-grid CSS class, 6 home section components, expanded message files in 3 locales, updated app/[locale]/page.tsx.
</objective>

<execution_context>
@/Users/shannendorothee/Projects/Website/.planning/phases/02-home-navigation/02-02-PLAN.md
</execution_context>

<context>
@/Users/shannendorothee/Projects/Website/.planning/PROJECT.md
@/Users/shannendorothee/Projects/Website/.planning/ROADMAP.md
@/Users/shannendorothee/Projects/Website/CLAUDE.md

<prerequisite_gate>
BEFORE executing any task, verify PLAN-01 output:
  test -f /Users/shannendorothee/Projects/Website/i18n/navigation.ts
  grep '"use client"' /Users/shannendorothee/Projects/Website/components/Navbar.tsx
  grep 'createNavigation' /Users/shannendorothee/Projects/Website/i18n/navigation.ts
  grep 'setRequestLocale' /Users/shannendorothee/Projects/Website/app/\[locale\]/layout.tsx

If any check fails: STOP. Execute Phase 2 PLAN-01 first.
</prerequisite_gate>

<interfaces>
From i18n/navigation.ts (PLAN-01 output):
  export const { Link, redirect, usePathname, useRouter } = createNavigation(routing);
  Import path for components: "@/i18n/navigation"
  Link auto-prefixes locale — href="/work" renders as /en/work, /id/work, etc.

From config/site.ts:
  export const siteConfig = { name: "SDT", tagline: "...", email: "sdttech.co@gmail.com" }
  Used in HeroSection and FooterCTA for mailto href.

From app/[locale]/page.tsx (current state — Phase 1 scaffold):
  export default function Home() { return <div>— scaffold ready —</div>; }
  Must be replaced with async function accepting params: Promise<{ locale: string }>.

Tailwind tokens (from tailwind.config.ts — confirmed present):
  bg-background    = #0a0a0a  (hero section base + hero CTA text)
  bg-surface       = #111111  (SolutionsStrip section, work teaser card, solution cards)
  bg-surface-2     = #1a1a1a  (FooterCTA section)
  bg-topbar        = #1a1a1a  (existing)
  bg-volt-green    = #96D02C  (HERO CTA ONLY — one per page)
  bg-volt-strong   = #557A12  (hero CTA hover — CONFIRMED in tailwind.config.ts)
  bg-grid-violet/10 = grid-violet at 10% opacity (solution card tags)
  bg-ev-teal/10    = ev-teal at 10% opacity (work teaser status tag)
  text-cyber-jade  = #008684
  text-jade-strong = #007069  (work teaser link hover — CONFIRMED in tailwind.config.ts)
  text-grid-violet = solution card tag text
  text-ev-teal     = work teaser status tag text
  text-text-secondary = #a1a1aa
  border-border-subtle = #2a2a2a
  max-w-content    = 1200px container

Note on hover tokens: `volt-strong` (#557A12) and `jade-strong` (#007069) are both registered in tailwind.config.ts. The classes `hover:bg-volt-strong` in HeroSection and `hover:text-jade-strong` in WorkTeaser are valid and will compile correctly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add .dot-grid CSS class and expand translation keys in all three locale files</name>
  <files>
    app/globals.css,
    messages/en.json,
    messages/id.json,
    messages/zh.json
  </files>
  <read_first>
    - /Users/shannendorothee/Projects/Website/app/globals.css — read current full file; append .dot-grid after the ::selection block without modifying existing rules
    - /Users/shannendorothee/Projects/Website/messages/en.json — read current full file; understand existing Phase 1 keys before replacing the home namespace and adding new namespaces
    - /Users/shannendorothee/Projects/Website/messages/id.json — read current full file; note _reviewNote key and position
    - /Users/shannendorothee/Projects/Website/messages/zh.json — read current full file; note _reviewNote key and position
    - /Users/shannendorothee/Projects/Website/.planning/phases/02-home-navigation/02-PATTERNS.md — sections "messages/en.json", "messages/id.json", "messages/zh.json" for the complete expanded schemas
  </read_first>
  <action>
    STEP A — globals.css:
    Append the following block to the end of `/Users/shannendorothee/Projects/Website/app/globals.css`, after the `::selection` block:

    ```
    /* Hero section dot-grid background — static-export safe pure CSS */
    /* Dot color #2a2a2a matches border-subtle token. Tailwind tokens unavailable in CSS files. */
    .dot-grid {
      background-image: radial-gradient(circle, #2a2a2a 1px, transparent 1px);
      background-size: 24px 24px;
    }
    ```

    Do NOT modify any existing CSS rules. Only append.

    STEP B — messages/en.json:
    Replace the entire file with the complete expanded schema from PATTERNS.md section "messages/en.json". The schema includes these top-level namespaces: nav, home, solutions, work, about, approach, footer, common.

    Key values for the `home` namespace (replace existing Phase 1 skeleton home keys):
    - eyebrow: "INTELLIGENT OPERATIONS"
    - heroLine1: "Infrastructure for"
    - heroLine2: "intelligent ops."
    - heroSub: "We build the software that runs fleet, logistics, and industrial operations in the real world."
    - ctaLabel: "Start a conversation"
    - solutionsEyebrow: "WHAT WE BUILD"
    - solutionsHeading: "Four capabilities. One platform."
    - approachEyebrow: "HOW WE WORK"
    - approachHeading: "Built differently, on purpose."
    - workEyebrow: "OUR WORK"
    - workHeading: "Shipped to production."
    - seeOurWork: "See our work →"
    - footerCtaHeading: "Want to work with us?"
    - footerCtaSub: "We take a small number of clients. If your problem is hard and the stakes are real, let's talk."
    - footerCtaButton: "Get in touch"

    The `solutions` namespace includes card1Title through card4Tag (4 cards). The `approach` namespace includes item1Title, item1Body, item2Title, item2Body, item3Title, item3Body. See PATTERNS.md for the full exact JSON to use.

    STEP C — messages/id.json:
    Replace the entire file with the Indonesian translation from PATTERNS.md section "messages/id.json".
    The _reviewNote key MUST be the first key: `"_reviewNote": "Auto-translated scaffolding — owner review required before launch"`.
    Preserve identical JSON structure (same namespaces, same keys) as en.json.

    STEP D — messages/zh.json:
    Replace the entire file with the Chinese Simplified translation from PATTERNS.md section "messages/zh.json".
    The _reviewNote key MUST be the first key: `"_reviewNote": "Auto-translated scaffolding — owner review required before launch"`.
    Preserve identical JSON structure (same namespaces, same keys) as en.json.

    CRITICAL: Every key that exists in en.json MUST exist in id.json and zh.json with the same namespace path. Missing keys cause next-intl to throw at build time.
  </action>
  <verify>
    <automated>grep -n '\.dot-grid' /Users/shannendorothee/Projects/Website/app/globals.css</automated>
    <automated>grep -n 'radial-gradient' /Users/shannendorothee/Projects/Website/app/globals.css</automated>
    <automated>grep -n '"footerCtaHeading"' /Users/shannendorothee/Projects/Website/messages/en.json /Users/shannendorothee/Projects/Website/messages/id.json /Users/shannendorothee/Projects/Website/messages/zh.json</automated>
    <automated>grep -n '"heroLine1"\|"heroLine2"\|"ctaLabel"' /Users/shannendorothee/Projects/Website/messages/en.json</automated>
    <automated>grep -n '"item1Title"\|"item2Title"\|"item3Title"' /Users/shannendorothee/Projects/Website/messages/en.json</automated>
    <automated>grep -n '"card1Title"\|"card2Title"\|"card3Title"\|"card4Title"' /Users/shannendorothee/Projects/Website/messages/en.json</automated>
    <automated>grep -n '_reviewNote' /Users/shannendorothee/Projects/Website/messages/id.json /Users/shannendorothee/Projects/Website/messages/zh.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '\.dot-grid' app/globals.css` returns a result with `.dot-grid {`
    - `grep -n 'radial-gradient' app/globals.css` returns `background-image: radial-gradient(circle, #2a2a2a 1px, transparent 1px)`
    - `grep -n 'background-size: 24px 24px' app/globals.css` returns a result
    - `messages/en.json` contains `"footerCtaHeading"`, `"heroLine1"`, `"heroLine2"`, `"ctaLabel"`, `"seeOurWork"` keys
    - `messages/en.json` contains `"approach"` namespace with `"item1Title"`, `"item2Title"`, `"item3Title"`
    - `messages/en.json` contains `"solutions"` namespace with `"card1Title"` through `"card4Tag"`
    - `messages/id.json` contains identical namespaces and keys as `en.json` (same structure, Indonesian values)
    - `messages/zh.json` contains identical namespaces and keys as `en.json` (same structure, Chinese values)
    - `messages/id.json` first key is `_reviewNote` with value "Auto-translated scaffolding — owner review required before launch"
    - `messages/zh.json` first key is `_reviewNote` with same value
    - Existing globals.css rules (`@tailwind base`, `html {...}`, `::selection {...}`) are still present and unmodified
  </acceptance_criteria>
  <done>.dot-grid class appended to globals.css; messages/en.json, id.json, zh.json all have the full Phase 2 key set with matching structure across locales.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create all five home section components</name>
  <files>
    components/home/HeroSection.tsx,
    components/home/SolutionsStrip.tsx,
    components/home/SolutionCard.tsx,
    components/home/ApproachSection.tsx,
    components/home/WorkTeaser.tsx,
    components/home/FooterCTA.tsx
  </files>
  <read_first>
    - /Users/shannendorothee/Projects/Website/components/Footer.tsx — read as the role-match analog for Server Component naming and export patterns (named exports, no "use client")
    - /Users/shannendorothee/Projects/Website/components/LogoMark.tsx — read for the typed props interface pattern used in SolutionCard
    - /Users/shannendorothee/Projects/Website/config/site.ts — read to confirm the siteConfig export shape (needed in HeroSection and FooterCTA)
    - /Users/shannendorothee/Projects/Website/.planning/phases/02-home-navigation/02-PATTERNS.md — read sections for each component: HeroSection, SolutionsStrip, SolutionCard, ApproachSection, WorkTeaser, FooterCTA — these contain the complete JSX patterns
    - /Users/shannendorothee/Projects/Website/.planning/phases/02-home-navigation/02-UI-SPEC.md — read the Content Specification and Interactive States sections for exact copy values and class specifications
  </read_first>
  <action>
    Create six new files. All use named exports (not default exports). All section components are async Server Components using `getTranslations` from `"next-intl/server"` — NEVER `useTranslations` (that is the Client hook).

    SHARED PATTERNS for all components:
    - Import path for cross-directory: `@/` prefix always (e.g., `@/config/site`, `@/i18n/navigation`)
    - Named export: `export async function ComponentName()` for async Server Components
    - `SolutionCard` is the only synchronous component (receives pre-translated props, no getTranslations): `export function SolutionCard({ icon, title, desc, tag }: SolutionCardProps)`
    - Section vertical padding: `className="py-24 md:py-32 px-6"` on each section's outer element
    - Container: `<div className="mx-auto max-w-content">` inside each section
    - Transition: `transition-colors duration-150 ease-out` on all interactive elements
    - Focus ring: `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cyber-jade` on all interactive elements

    FILE 1 — `components/home/HeroSection.tsx`:
    - Imports: `getTranslations from "next-intl/server"`, `siteConfig from "@/config/site"`
    - Export: `export async function HeroSection()`
    - Outer element: `<section className="dot-grid bg-background min-h-screen flex flex-col items-center justify-center py-24 md:py-32 px-6">`
    - Eyebrow: `<p className="text-xs font-medium tracking-widest text-text-secondary uppercase mb-6">{t("eyebrow")}</p>`
    - H1: `<h1 className="text-5xl md:text-6xl lg:text-7xl font-bold tracking-tighter leading-none text-center mb-6 max-w-content">` with two `<span className="block">` children: `{t("heroLine1")}` and `{t("heroLine2")}`
    - Sub: `<p className="text-base font-medium leading-relaxed text-text-secondary text-center max-w-2xl mb-10">{t("heroSub")}</p>`
    - CTA: `<a href={\`mailto:${siteConfig.email}\`} className="bg-volt-green text-background font-medium px-6 py-3 rounded-md text-sm hover:bg-volt-strong transition-colors duration-150 ease-out focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-volt-green">{t("ctaLabel")}</a>`
    - Use `const t = await getTranslations("home");`
    - `volt-strong` (#557A12) is a registered token in tailwind.config.ts — `hover:bg-volt-strong` is valid
    - Do NOT add any other volt-green element. This is the ONLY volt-green element on the entire page.

    FILE 2 — `components/home/SolutionCard.tsx`:
    - Synchronous component (no async, no getTranslations — receives translated strings as props)
    - Props interface: `interface SolutionCardProps { icon: string; title: string; desc: string; tag: string; }`
    - Export: `export function SolutionCard({ icon, title, desc, tag }: SolutionCardProps)`
    - Card wrapper: `<div className="bg-surface border border-border-subtle rounded-lg p-6 flex flex-col gap-4 hover:border-cyber-jade transition-colors duration-150 ease-out">`
    - Icon: `<span className="text-2xl" aria-hidden="true">{icon}</span>`
    - Title: `<h3 className="text-lg font-bold tracking-tight leading-snug">{title}</h3>`
    - Desc: `<p className="text-base font-medium leading-relaxed text-text-secondary flex-1">{desc}</p>`
    - Tag: `<span className="self-start text-xs font-medium bg-grid-violet/10 text-grid-violet px-2 py-1 rounded">{tag}</span>`

    FILE 3 — `components/home/SolutionsStrip.tsx`:
    - Imports: `getTranslations from "next-intl/server"`, `SolutionCard from "@/components/home/SolutionCard"`
    - Export: `export async function SolutionsStrip()`
    - Use `const t = await getTranslations("home");` and `const st = await getTranslations("solutions");`
    - Define cards array inside the function:
      ```
      const cards = [
        { icon: "⚡", titleKey: "card1Title", descKey: "card1Desc", tagKey: "card1Tag" },
        { icon: "◈",  titleKey: "card2Title", descKey: "card2Desc", tagKey: "card2Tag" },
        { icon: "⊞",  titleKey: "card3Title", descKey: "card3Desc", tagKey: "card3Tag" },
        { icon: "∿",  titleKey: "card4Title", descKey: "card4Desc", tagKey: "card4Tag" },
      ] as const;
      ```
    - Section wrapper: `<section className="py-24 md:py-32 px-6 bg-background">`
    - Eyebrow: `<p className="text-xs font-medium tracking-widest text-text-secondary uppercase mb-4">{t("solutionsEyebrow")}</p>`
    - Heading: `<h2 className="text-3xl md:text-4xl font-bold tracking-tight leading-tight mb-12">{t("solutionsHeading")}</h2>`
    - Grid: `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 lg:gap-6">` mapping each card to `<SolutionCard key={card.titleKey} icon={card.icon} title={st(card.titleKey)} desc={st(card.descKey)} tag={st(card.tagKey)} />`

    FILE 4 — `components/home/ApproachSection.tsx`:
    - Imports: `getTranslations from "next-intl/server"`
    - Export: `export async function ApproachSection()`
    - Use `const t = await getTranslations("home");` and `const at = await getTranslations("approach");`
    - Define items array inside function:
      ```
      const items = [
        { num: "01", titleKey: "item1Title", bodyKey: "item1Body" },
        { num: "02", titleKey: "item2Title", bodyKey: "item2Body" },
        { num: "03", titleKey: "item3Title", bodyKey: "item3Body" },
      ] as const;
      ```
    - Section wrapper: `<section className="py-24 md:py-32 px-6 bg-surface">`
    - Eyebrow + heading as in SolutionsStrip pattern
    - Grid: `<div className="flex flex-col gap-8 md:grid md:grid-cols-3 md:gap-8">` mapping each item
    - Each item div: `<div className="flex flex-col gap-3">` containing:
      - Number accent: `<span className="text-4xl font-bold text-cyber-jade/20 leading-none">{item.num}</span>`
      - Title: `<h3 className="text-lg font-bold tracking-tight leading-snug">{at(item.titleKey)}</h3>`
      - Body: `<p className="text-base font-medium leading-relaxed text-text-secondary">{at(item.bodyKey)}</p>`

    FILE 5 — `components/home/WorkTeaser.tsx`:
    - Imports: `getTranslations from "next-intl/server"`, `Link from "@/i18n/navigation"` (locale-aware Link)
    - Props interface: `interface WorkTeaserProps { locale: string; }` (locale is passed from page but Link handles prefix automatically)
    - Export: `export async function WorkTeaser({ locale }: WorkTeaserProps)`
    - Use `const t = await getTranslations("home");` and `const wt = await getTranslations("work");`
    - Section wrapper: `<section className="py-24 md:py-32 px-6 bg-background">`
    - Eyebrow (`workEyebrow`) + heading (`workHeading`) inside max-w-content container
    - Card: `<div className="max-w-2xl mx-auto bg-surface border border-border-subtle rounded-lg p-6 flex flex-col gap-4 mb-8">` containing:
      - Status tag: `<span className="self-start text-xs font-medium bg-ev-teal/10 text-ev-teal px-2 py-1 rounded">Live</span>`
      - Title: `<h3 className="text-lg font-bold tracking-tight leading-snug">{wt("evFleetTitle")}</h3>`
      - Desc: `<p className="text-base font-medium leading-relaxed text-text-secondary">{wt("evFleetDesc")}</p>`
    - "See our work" link after card (same max-w-2xl mx-auto div):
      `<Link href="/work" className="text-cyber-jade text-sm font-medium hover:text-jade-strong transition-colors duration-150 ease-out focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cyber-jade rounded-sm">{t("seeOurWork")}</Link>`
    - `jade-strong` (#007069) is a registered token in tailwind.config.ts — `hover:text-jade-strong` is valid
    - Note: `Link` from `@/i18n/navigation` auto-prefixes — `href="/work"` renders as `/en/work`, etc.

    FILE 6 — `components/home/FooterCTA.tsx`:
    - Imports: `getTranslations from "next-intl/server"`, `siteConfig from "@/config/site"`
    - Export: `export async function FooterCTA()`
    - Use `const t = await getTranslations("home");`
    - Section wrapper: `<section className="py-24 md:py-32 px-6 bg-surface-2 border-t border-border-subtle">`
    - Inner: `<div className="mx-auto max-w-content text-center flex flex-col items-center gap-6">`
    - Heading: `<h2 className="text-3xl md:text-4xl font-bold tracking-tight leading-tight">{t("footerCtaHeading")}</h2>`
    - Sub: `<p className="text-base font-medium leading-relaxed text-text-secondary max-w-xl">{t("footerCtaSub")}</p>`
    - CTA button: `<a href={\`mailto:${siteConfig.email}\`} className="border border-cyber-jade text-cyber-jade rounded-md px-6 py-3 text-sm font-medium hover:bg-cyber-jade/10 transition-colors duration-150 ease-out focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cyber-jade">{t("footerCtaButton")}</a>`
    - MUST use `border-cyber-jade text-cyber-jade` — NOT `bg-volt-green`. Volt Green is already consumed by HeroSection.

    CRITICAL VOLT GREEN RULE:
    - `bg-volt-green` and `text-volt-green` appear ONLY in HeroSection.tsx — nowhere else
    - Run `grep -r "volt-green" components/home/` after creating files; only HeroSection.tsx should match
  </action>
  <verify>
    <automated>grep -rn 'volt-green' /Users/shannendorothee/Projects/Website/components/home/</automated>
    <automated>grep -n 'bg-volt-green' /Users/shannendorothee/Projects/Website/components/home/HeroSection.tsx</automated>
    <automated>grep -n 'hover:bg-volt-strong' /Users/shannendorothee/Projects/Website/components/home/HeroSection.tsx</automated>
    <automated>grep -n 'bg-ev-teal/10 text-ev-teal' /Users/shannendorothee/Projects/Website/components/home/WorkTeaser.tsx</automated>
    <automated>grep -n 'hover:text-jade-strong' /Users/shannendorothee/Projects/Website/components/home/WorkTeaser.tsx</automated>
    <automated>grep -n 'border-cyber-jade text-cyber-jade' /Users/shannendorothee/Projects/Website/components/home/FooterCTA.tsx</automated>
    <automated>grep -n 'bg-grid-violet/10 text-grid-violet' /Users/shannendorothee/Projects/Website/components/home/SolutionCard.tsx</automated>
    <automated>grep -n 'text-cyber-jade/20' /Users/shannendorothee/Projects/Website/components/home/ApproachSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'volt-green' components/home/` returns results ONLY from `HeroSection.tsx` (no other home component has volt-green)
    - `components/home/HeroSection.tsx` contains `bg-volt-green` and `text-background` on the CTA anchor
    - `components/home/HeroSection.tsx` contains `hover:bg-volt-strong` (valid registered token)
    - `components/home/HeroSection.tsx` contains `dot-grid` in the section className
    - `components/home/SolutionCard.tsx` contains `bg-grid-violet/10 text-grid-violet`
    - `components/home/ApproachSection.tsx` contains `text-cyber-jade/20`
    - `components/home/WorkTeaser.tsx` contains `bg-ev-teal/10 text-ev-teal`
    - `components/home/WorkTeaser.tsx` contains `hover:text-jade-strong` (valid registered token)
    - `components/home/FooterCTA.tsx` contains `border border-cyber-jade text-cyber-jade`
    - `components/home/FooterCTA.tsx` does NOT contain `volt-green`
    - All six new component files use named exports (`export async function` or `export function`), not `export default function`
  </acceptance_criteria>
  <done>All five home sections and SolutionCard created as Server Components. Hover tokens volt-strong and jade-strong are confirmed registered in tailwind.config.ts and used correctly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Assemble app/[locale]/page.tsx with all home sections</name>
  <files>
    app/[locale]/page.tsx
  </files>
  <read_first>
    - /Users/shannendorothee/Projects/Website/app/[locale]/page.tsx — read current scaffold to understand the existing async/params structure before replacing it
    - /Users/shannendorothee/Projects/Website/.planning/phases/02-home-navigation/02-PATTERNS.md — section "app/[locale]/page.tsx" for the complete replacement pattern
  </read_first>
  <action>
    Replace the entire content of `/Users/shannendorothee/Projects/Website/app/[locale]/page.tsx` with the async Server Component that composes all five home sections.

    The file must:
    1. Import `setRequestLocale` from `"next-intl/server"`
    2. Import all 5 section components from `@/components/home/...`:
       - `import { HeroSection } from "@/components/home/HeroSection";`
       - `import { SolutionsStrip } from "@/components/home/SolutionsStrip";`
       - `import { ApproachSection } from "@/components/home/ApproachSection";`
       - `import { WorkTeaser } from "@/components/home/WorkTeaser";`
       - `import { FooterCTA } from "@/components/home/FooterCTA";`
    3. Export default async function with the params-as-Promise signature:
       `export default async function Home({ params }: { params: Promise<{ locale: string }> })`
    4. First two lines inside the function body:
       `const { locale } = await params;`
       `setRequestLocale(locale);`
    5. Return JSX with all five sections in order:
       ```
       return (
         <>
           <HeroSection />
           <SolutionsStrip />
           <ApproachSection />
           <WorkTeaser locale={locale} />
           <FooterCTA />
         </>
       );
       ```
    6. No `"use client"` directive — this is a Server Component
    7. Keep `generateStaticParams` if it exists in the Phase 1 scaffold; if not, do NOT add it (it belongs in the layout per Phase 1 patterns)

    After writing the file, run `npm run build` to confirm all TypeScript types resolve and the static export completes without errors.
  </action>
  <verify>
    <automated>grep -n 'setRequestLocale' /Users/shannendorothee/Projects/Website/app/\[locale\]/page.tsx</automated>
    <automated>grep -n '"use client"' /Users/shannendorothee/Projects/Website/app/\[locale\]/page.tsx</automated>
    <automated>grep -n 'await params' /Users/shannendorothee/Projects/Website/app/\[locale\]/page.tsx</automated>
    <automated>grep -n 'HeroSection\|SolutionsStrip\|ApproachSection\|WorkTeaser\|FooterCTA' /Users/shannendorothee/Projects/Website/app/\[locale\]/page.tsx</automated>
    <automated>cd /Users/shannendorothee/Projects/Website && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `app/[locale]/page.tsx` contains `setRequestLocale(locale)`
    - `app/[locale]/page.tsx` does NOT contain `"use client"`
    - `app/[locale]/page.tsx` contains `await params` (not just `params` — must destructure from Promise)
    - `app/[locale]/page.tsx` imports and renders all 5 section components
    - `npm run build` exits 0
    - Build output includes static pages for `/en/`, `/id/`, `/zh/`
  </acceptance_criteria>
  <done>app/[locale]/page.tsx updated with setRequestLocale and all five home sections composed. Build passes. Home page fully live at /en, /id, /zh with translated content.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 added .dot-grid CSS and expanded translation keys across all 3 locale files. Task 2 created HeroSection, SolutionsStrip, SolutionCard, ApproachSection, WorkTeaser, FooterCTA. Task 3 updated app/[locale]/page.tsx with setRequestLocale and all sections composed.
  </what-built>
  <how-to-verify>
    Run: `npm run dev` in the project directory, then open http://localhost:3000/en in a browser.

    Check these things:
    1. Hero section: dot-grid pattern is visible in the background (subtle dark dots on black), eyebrow reads "INTELLIGENT OPERATIONS", H1 shows 2 lines ("Infrastructure for" / "intelligent ops."), sub-line is present, CTA button is Volt Green (bright yellow-green color)
    2. Solutions strip: 4 cards appear in a grid; each has an icon, title, description, and a small purple/violet tag; hover over a card — border shifts to cyan/jade color
    3. Approach section: shows "HOW WE WORK" eyebrow, heading, 3 items with large faded cyan numbers (01, 02, 03) and titles/body text
    4. Work teaser: shows "OUR WORK" eyebrow, heading, the EV Fleet Operations card with a teal "Live" status tag; "See our work →" link is present in cyan
    5. Footer CTA: shows "Want to work with us?" heading, sub-line, and a cyber-jade OUTLINE button (cyan border + text — NOT filled green)
    6. Check that Volt Green (bright yellow-green) appears ONLY on the hero CTA button. All other buttons/links should be cyber-jade (teal/cyan) — especially the footer CTA.
    7. Visit http://localhost:3000/id — all text is in Indonesian (e.g., hero heading should show "Infrastruktur untuk" / "operasi cerdas.")
    8. Visit http://localhost:3000/zh — all text is in Chinese (e.g., hero heading should show Chinese characters)
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 checks pass, or describe which checks failed and what was observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server→Client | All home sections are Server Components; no user input crosses this boundary in Phase 2 |
| mailto: href | siteConfig.email is the source for all mailto links; never constructed from user-controlled values |
| Translation values | next-intl renders translation values as React text nodes (not innerHTML); XSS from translation files is not possible via the standard React rendering path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02B-01 | Information Disclosure | `siteConfig.email` in HeroSection and FooterCTA mailto hrefs | accept | Email is intentionally public (marketing contact); this is the intended disclosure |
| T-02B-02 | Injection | Translation values in messages/*.json rendered via next-intl | accept | next-intl passes values to React as text nodes — React escapes all HTML; XSS is not possible through this path |
| T-02B-03 | Spoofing | Locale parameter in app/[locale]/page.tsx `await params` | accept | Locale is validated at the routing layer by next-intl's routing config; only "en", "id", "zh" are valid — invalid locales return 404 |
| T-02B-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 2 — zero supply chain surface; already audited in Phase 1 |
</threat_model>

<verification>
After all tasks complete and checkpoint is approved:

  cd /Users/shannendorothee/Projects/Website && npm run build

Expected: exits 0, static pages emitted for at minimum /en/, /id/, /zh/.

Volt Green check (must return output ONLY from HeroSection.tsx):
  grep -rn 'volt-green' components/home/

FooterCTA not using volt-green (must return 0):
  grep -c 'volt-green' components/home/FooterCTA.tsx

Translation key parity check (must return matches in all three files):
  grep -c '"footerCtaHeading"' messages/en.json messages/id.json messages/zh.json

Dot-grid CSS (must return result):
  grep -n 'radial-gradient' app/globals.css

setRequestLocale in page (must return result):
  grep -n 'setRequestLocale' app/\[locale\]/page.tsx

Hover token sanity check (volt-strong and jade-strong are registered in tailwind.config.ts):
  grep 'volt-strong\|jade-strong' /Users/shannendorothee/Projects/Website/tailwind.config.ts
</verification>

<success_criteria>
- .dot-grid CSS class added to globals.css with radial-gradient(circle, #2a2a2a 1px, transparent 1px) and background-size: 24px 24px
- messages/en.json, id.json, zh.json all have identical key structure with Phase 2 keys (footerCtaHeading, heroLine1, heroLine2, seeOurWork, approach namespace, solutions cards)
- HeroSection.tsx uses bg-volt-green text-background on CTA and hover:bg-volt-strong — this is the ONLY volt-green element on the page
- SolutionCard.tsx uses bg-grid-violet/10 text-grid-violet on tags
- ApproachSection.tsx uses text-cyber-jade/20 on numbered accents
- WorkTeaser.tsx uses bg-ev-teal/10 text-ev-teal on status tag; Link from @/i18n/navigation for /work href; hover:text-jade-strong on "See our work" link
- FooterCTA.tsx uses border border-cyber-jade text-cyber-jade — NOT volt-green
- app/[locale]/page.tsx calls setRequestLocale(locale) and is async with await params
- npm run build exits 0 with /en/, /id/, /zh/ pages emitted
- Human checkpoint: all 8 visual checks pass
</success_criteria>

<output>
Create `/Users/shannendorothee/Projects/Website/.planning/phases/02-home-navigation/02-02-SUMMARY.md` when done.
</output>
