---
phase: 04-launch-prep
plan: 04
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - public/sitemap.xml
  - public/robots.txt
  - app/not-found.tsx
  - app/[locale]/not-found.tsx
autonomous: true
requirements:
  - LAUNCH-04
  - LAUNCH-05

must_haves:
  truths:
    - "Search crawlers can discover all 24 URLs via /sitemap.xml"
    - "robots.txt permits full crawl and points to the sitemap"
    - "Navigating to /invalid-path shows the branded 404 page (root not-found catches it)"
    - "Navigating to /en/invalid-subpath shows the branded 404 page (locale not-found catches it)"
    - "The 404 page displays: LogoMark, '404' code, 'Page not found' heading, body copy, 'Back to home' link"
  artifacts:
    - path: "public/sitemap.xml"
      provides: "24 URLs with trailing slashes, priorities, monthly changefreq"
      contains: "<urlset"
    - path: "public/robots.txt"
      provides: "Allow all crawlers, point to sitemap"
      contains: "Sitemap: https://sdt.technology/sitemap.xml"
    - path: "app/not-found.tsx"
      provides: "Root-level 404 for paths outside [locale] segment"
    - path: "app/[locale]/not-found.tsx"
      provides: "Branded 404 upgraded from stub to full spec"
      contains: "LogoMark"
  key_links:
    - from: "public/sitemap.xml"
      to: "all 24 route URLs"
      via: "<loc> entries with trailing slashes"
      pattern: "sdt\\.technology.*/$"
    - from: "app/not-found.tsx"
      to: "app/[locale]/not-found.tsx"
      via: "identical visual spec, root version has no locale dependency"
      pattern: "LogoMark"
---

<objective>
Create public/sitemap.xml (24 URLs, all with trailing slashes), public/robots.txt (Allow + Sitemap reference), upgrade app/[locale]/not-found.tsx to the full branded spec from UI-SPEC, and create new app/not-found.tsx for root-level 404s.

Purpose: SEO files make the site discoverable by crawlers on day one. The branded 404 page ensures any invalid URL is caught gracefully with on-brand copy rather than a blank Next.js default page.

Output:
- public/sitemap.xml — 24 loc entries
- public/robots.txt — 3 lines
- app/[locale]/not-found.tsx — upgraded with LogoMark, heading, body, correct link text
- app/not-found.tsx — new file, same visual spec as locale not-found
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- LogoMark component signature (from components/LogoMark.tsx): -->
<!--   export function LogoMark({ size = 32, className }: LogoMarkProps) -->
<!--   Use size={40} for the 404 page per UI-SPEC -->

<!-- Existing app/[locale]/not-found.tsx (lines 1-17): -->
<!--   import Link from "next/link";                                                -->
<!--   import { siteConfig } from "@/config/site";                                 -->
<!--   export default function NotFound() {                                         -->
<!--     return (                                                                   -->
<!--       <div className="flex flex-col items-center justify-center min-h-[60vh] gap-4 text-center px-6"> -->
<!--         <p className="text-text-muted text-sm tracking-widest font-mono">404</p> -->
<!--         <p className="text-text-secondary text-base">This page does not exist.</p> -->
<!--         <Link href="/" className="text-cyber-jade text-sm hover:text-jade-strong transition-colors duration-150 ease-out"> -->
<!--           Return to {siteConfig.name} tech                                      -->
<!--         </Link>                                                                -->
<!--       </div>                                                                   -->
<!--     );                                                                         -->
<!--   }                                                                            -->

<!-- UI-SPEC branded 404 requirements: -->
<!--   Container: flex flex-col items-center justify-center min-h-[60vh] gap-8 text-center px-6 (gap-4 → gap-8) -->
<!--   Add LogoMark at size={40} above error block -->
<!--   Error code p: "404" — text-sm font-mono text-text-muted tracking-widest (unchanged) -->
<!--   Add h1: "Page not found" — text-xl font-semibold text-white leading-[1.2] -->
<!--   Body p: "The URL you followed doesn't exist or has moved." — text-base text-text-secondary leading-[1.6] -->
<!--   CTA link: "Back to home" — text-sm text-cyber-jade hover:text-jade-strong transition-colors duration-150 ease-out -->
<!--   href="/", remove siteConfig import -->
<!--   No Volt Green, no box shadow -->

<!-- Sitemap URL list (all 24, confirmed from RESEARCH.md): -->
<!-- All trailing slashes required — next.config.mjs trailingSlash: true confirmed -->
<!-- home: /en/ /id/ /zh/ — priority 1.0 -->
<!-- solutions: /en/solutions/ /id/solutions/ /zh/solutions/ — priority 0.9 -->
<!-- fleet-mobility × 3 locales — priority 0.8 -->
<!-- iot-hardware × 3 locales — priority 0.8 -->
<!-- logistics-intelligence × 3 locales — priority 0.8 -->
<!-- ai-operations × 3 locales — priority 0.8 -->
<!-- work: /en/work/ /id/work/ /zh/work/ — priority 0.8 -->
<!-- about: /en/about/ /id/about/ /zh/about/ — priority 0.7 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create public/sitemap.xml and public/robots.txt</name>
  <files>
    public/sitemap.xml
    public/robots.txt
  </files>
  <read_first>
    - next.config.mjs — confirm trailingSlash: true (all loc URLs must end with /)
    - .planning/phases/04-launch-prep/04-RESEARCH.md — Pattern 7 (sitemap XML structure), complete 24-URL list in "SEO Files: Complete URL List" section, Pitfall 3 (trailing slash match)
    - .planning/phases/04-launch-prep/04-UI-SPEC.md — LAUNCH-04 section (robots.txt content, sitemap structure)
  </read_first>
  <action>
    Create public/sitemap.xml with the standard sitemap XML declaration and urlset. Include all 24 URL entries. Group them by priority in this order: home (3 URLs, priority 1.0), solutions index (3 URLs, priority 0.9), fleet-mobility × 3 (priority 0.8), iot-hardware × 3 (priority 0.8), logistics-intelligence × 3 (priority 0.8), ai-operations × 3 (priority 0.8), work × 3 (priority 0.8), about × 3 (priority 0.7).

    Each url entry must have: loc (full https://sdt.technology URL with trailing slash), changefreq (monthly), priority (number matching group above).

    The urlset xmlns must be "http://www.sitemaps.org/schemas/sitemap/0.9".

    Create public/robots.txt with exactly these 3 lines:
    User-agent: *
    Allow: /
    Sitemap: https://sdt.technology/sitemap.xml

    The public/ directory was created in Plan 04-01 Task 1. If it does not yet exist (Plans run in parallel), run: mkdir -p public
  </action>
  <verify>
    <automated>
      grep -c '<loc>' public/sitemap.xml &&
      grep -c 'sdt.technology/en/' public/sitemap.xml &&
      grep 'Sitemap:' public/robots.txt &&
      grep 'Allow: /' public/robots.txt
    </automated>
  </verify>
  <done>
    public/sitemap.xml exists and contains exactly 24 &lt;loc&gt; entries.
    Every &lt;loc&gt; value ends with a trailing slash (/).
    public/robots.txt exists and contains "Allow: /" and "Sitemap: https://sdt.technology/sitemap.xml".
    npm run build exits 0 (public/ files are copied verbatim to out/ in static export).
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade locale not-found and create root not-found</name>
  <files>
    app/[locale]/not-found.tsx
    app/not-found.tsx
  </files>
  <read_first>
    - app/[locale]/not-found.tsx — read current stub (all 17 lines) to understand what changes
    - components/LogoMark.tsx — confirm LogoMark named export and size prop
    - .planning/phases/04-launch-prep/04-UI-SPEC.md — LAUNCH-05 visual spec (container classes, element order, copy strings, color tokens)
    - .planning/phases/04-launch-prep/04-RESEARCH.md — 404 Page Upgrade Spec section (6-point diff from stub to spec), Pitfall 4 (root not-found.tsx needed)
    - tailwind.config.ts — confirm text-text-muted, text-text-secondary, text-cyber-jade, text-jade-strong tokens exist
  </read_first>
  <action>
    Rewrite app/[locale]/not-found.tsx with the following changes from the current stub:
    1. Remove siteConfig import (no longer needed)
    2. Add LogoMark import: import { LogoMark } from "@/components/LogoMark";
    3. Change container gap from gap-4 to gap-8
    4. Add &lt;LogoMark size={40} /&gt; as the first child element (above the error code paragraph)
    5. Add &lt;h1&gt; element after the error code paragraph: "Page not found" with classes text-xl font-semibold text-white leading-[1.2]
    6. Change body copy from "This page does not exist." to "The URL you followed doesn't exist or has moved." and add leading-[1.6] to the paragraph className
    7. Change Link text from "Return to {siteConfig.name} tech" to "Back to home"
    8. Keep href="/" on the Link (already correct in stub)
    9. Keep all existing className values on the Link unchanged

    Create app/not-found.tsx as a new file. It must:
    - Import Link from "next/link" and LogoMark from "@/components/LogoMark"
    - Export default function NotFound() with the identical JSX structure as the upgraded locale not-found
    - Use the same container classes, element order, copy strings, and Tailwind tokens
    - No locale-specific imports (no next-intl, no siteConfig)
    - href="/" on the return link

    Do not add metadata exports to not-found files — Next.js does not support metadata on not-found pages.
  </action>
  <verify>
    <automated>
      grep -c 'LogoMark' "app/[locale]/not-found.tsx" &&
      grep -c 'Page not found' "app/[locale]/not-found.tsx" &&
      grep -c 'Back to home' "app/[locale]/not-found.tsx" &&
      grep -c 'gap-8' "app/[locale]/not-found.tsx" &&
      grep -c 'LogoMark' app/not-found.tsx &&
      npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    app/[locale]/not-found.tsx contains: "LogoMark", "Page not found", "The URL you followed doesn't exist or has moved.", "Back to home", "gap-8", href="/".
    app/[locale]/not-found.tsx does NOT contain: "siteConfig", "Return to", "This page does not exist", "gap-4".
    app/not-found.tsx exists and contains "LogoMark" and "Page not found".
    npm run build exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crawler → sitemap.xml | Public read-only file; no execution surface |
| browser → not-found.tsx | Static error page; no user input, no server APIs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | sitemap.xml URL enumeration | accept | URLs are public routes; enumeration is the intended purpose of a sitemap |
| T-04-08 | Denial of Service | robots.txt Allow: / | accept | Site is a public marketing site; crawl restrictions would harm SEO |
| T-04-09 | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After all tasks complete:
- grep -c '&lt;loc&gt;' public/sitemap.xml outputs 24
- All loc entries end with trailing slash: grep -v 'sdt.technology.*/$' public/sitemap.xml | grep '&lt;loc&gt;' outputs nothing
- public/robots.txt contains "Sitemap: https://sdt.technology/sitemap.xml"
- app/not-found.tsx exists with LogoMark import
- app/[locale]/not-found.tsx contains "Page not found" and "Back to home"
- npm run build exits 0
</verification>

<success_criteria>
public/sitemap.xml lists all 24 URLs with trailing slashes. public/robots.txt allows all crawlers and references the sitemap. Both not-found files match the UI-SPEC visual spec (LogoMark, 404 code, "Page not found" heading, corrected body copy, "Back to home" link). Build passes.
</success_criteria>

<output>
Create .planning/phases/04-launch-prep/04-04-SUMMARY.md when done.
</output>
