---
phase: 04-launch-prep
plan: 03
type: execute
wave: 2
depends_on:
  - "04-02"
files_modified:
  - app/[locale]/page.tsx
  - app/[locale]/solutions/page.tsx
  - app/[locale]/solutions/[slug]/page.tsx
  - app/[locale]/work/page.tsx
  - app/[locale]/about/page.tsx
autonomous: true
requirements:
  - LAUNCH-03

must_haves:
  truths:
    - "Every route has a unique browser tab title distinct from every other route"
    - "Every route has a meta description that reflects its actual content"
    - "Home page title is the full branded string without a doubled suffix"
    - "Each solution detail page title reflects the specific solution slug, not a generic fallback"
  artifacts:
    - path: "app/[locale]/page.tsx"
      provides: "Home page metadata with title.absolute bypassing template"
      contains: "title.absolute"
    - path: "app/[locale]/solutions/page.tsx"
      provides: "Solutions index metadata — title: 'Solutions'"
      contains: "export const metadata"
    - path: "app/[locale]/solutions/[slug]/page.tsx"
      provides: "Dynamic metadata keyed by slug via generateMetadata"
      contains: "export async function generateMetadata"
    - path: "app/[locale]/work/page.tsx"
      provides: "Work page metadata — title: 'Work'"
      contains: "export const metadata"
    - path: "app/[locale]/about/page.tsx"
      provides: "About page metadata — title: 'About'"
      contains: "export const metadata"
  key_links:
    - from: "app/[locale]/solutions/[slug]/page.tsx generateMetadata"
      to: "slugMeta lookup table"
      via: "await params then slugMeta[slug]"
      pattern: "await params.*slug"
    - from: "app/layout.tsx title.template"
      to: "app/[locale]/page.tsx title.absolute"
      via: "Next.js metadata merge — absolute bypasses template"
      pattern: "title.*absolute"
---

<objective>
Add unique title and description metadata exports to all 5 page routes. Home uses title.absolute to bypass the template. Solution detail uses generateMetadata with an inline slugMeta lookup. All other pages use static metadata exports.

Purpose: Plan 04-02 set the root layout title template ("%s — SDT tech") and OG defaults. This plan fills in the per-page values so every route has a distinct, crawlable title and description. Per-page metadata must only set title and description — NOT openGraph — to avoid the shallow-merge pitfall that would wipe the inherited OG image.

Output: 5 modified page files, each with a metadata export or generateMetadata function.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-launch-prep/04-02-SUMMARY.md

<interfaces>
<!-- Root layout title template (set by Plan 04-02): -->
<!--   template: "%s — SDT tech" -->
<!--   Page-level title string "Solutions" → browser renders "Solutions — SDT tech" -->
<!--   Page-level title.absolute bypasses the template entirely -->

<!-- Per-page metadata values (exact strings from RESEARCH.md and UI-SPEC): -->
<!--   Home:  title.absolute = "SDT tech — Infrastructure for intelligent operations." -->
<!--          description = "SDT tech builds hardware-aware fleet mobility, IoT, logistics, and AI operations systems for enterprise operators." -->
<!--   Solutions index: title = "Solutions" -->
<!--          description = "Four production-grade systems covering fleet mobility, IoT hardware, logistics intelligence, and AI-driven operations." -->
<!--   Fleet Mobility: title = "Fleet Mobility" -->
<!--          description = "Real-time fleet tracking and dispatch for EV and mixed fleets. Built for operators managing high-utilisation vehicles at scale." -->
<!--   IoT Hardware: title = "IoT Hardware" -->
<!--          description = "Industrial IoT devices and edge firmware for connected operations. Hardened for field deployment in transport and logistics." -->
<!--   Logistics Intelligence: title = "Logistics Intelligence" -->
<!--          description = "Data pipelines and routing logic that turn raw fleet telemetry into operational decisions. Built for last-mile and mid-mile operations." -->
<!--   AI Operations: title = "AI Operations" -->
<!--          description = "Machine learning systems that surface anomalies, forecast demand, and automate triage for complex transport operations." -->
<!--   Work: title = "Work" -->
<!--          description = "Production deployments built by SDT tech. Starting with EV Fleet Operations — a live system managing electric vehicle fleets." -->
<!--   About: title = "About" -->
<!--          description = "SDT tech builds infrastructure-grade software for operators who run complex systems at scale. Jakarta-based. Production-focused." -->

<!-- CRITICAL: Do NOT set openGraph on any page-level metadata object. -->
<!-- Next.js metadata merge is SHALLOW — any openGraph key in a child wipes all openGraph keys -->
<!-- from the parent, losing the og.png image reference set in the root layout. -->
<!-- Only set: title (or title.absolute) and description at page level. -->

<!-- generateMetadata params pattern (from RESEARCH.md Pitfall 5 and Pattern 4): -->
<!--   export async function generateMetadata({ params }: { params: Promise<{ locale: string; slug: string }> }): Promise<Metadata> -->
<!--   const { slug } = await params   ← must use await, not synchronous access -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add static metadata to home, solutions index, work, and about page files</name>
  <files>
    app/[locale]/page.tsx
    app/[locale]/solutions/page.tsx
    app/[locale]/work/page.tsx
    app/[locale]/about/page.tsx
  </files>
  <read_first>
    - app/[locale]/page.tsx — read full file to find insertion point (after imports, before generateStaticParams or default export)
    - app/[locale]/solutions/page.tsx — same
    - app/[locale]/work/page.tsx — same
    - app/[locale]/about/page.tsx — same
    - .planning/phases/04-launch-prep/04-RESEARCH.md — Pattern 2 (static metadata), Pattern 3 (home page title.absolute), Pitfall 1 (do not set openGraph at page level)
  </read_first>
  <action>
    In each of the four page files, add a metadata import and export. Place imports at top of file with existing imports. Place the metadata export after imports and before generateStaticParams (or before the default export if generateStaticParams is absent).

    Add "import type { Metadata } from 'next';" to each file if not already present.

    app/[locale]/page.tsx — use title.absolute to bypass the template:
    export const metadata: Metadata = {
      title: { absolute: "SDT tech — Infrastructure for intelligent operations." },
      description: "SDT tech builds hardware-aware fleet mobility, IoT, logistics, and AI operations systems for enterprise operators.",
    };

    app/[locale]/solutions/page.tsx:
    export const metadata: Metadata = {
      title: "Solutions",
      description: "Four production-grade systems covering fleet mobility, IoT hardware, logistics intelligence, and AI-driven operations.",
    };

    app/[locale]/work/page.tsx:
    export const metadata: Metadata = {
      title: "Work",
      description: "Production deployments built by SDT tech. Starting with EV Fleet Operations — a live system managing electric vehicle fleets.",
    };

    app/[locale]/about/page.tsx:
    export const metadata: Metadata = {
      title: "About",
      description: "SDT tech builds infrastructure-grade software for operators who run complex systems at scale. Jakarta-based. Production-focused.",
    };

    Do NOT add any openGraph, twitter, or images fields to these metadata exports.
  </action>
  <verify>
    <automated>
      grep -c 'title.*absolute' "app/[locale]/page.tsx" &&
      grep -c 'export const metadata' "app/[locale]/solutions/page.tsx" &&
      grep -c 'export const metadata' "app/[locale]/work/page.tsx" &&
      grep -c 'export const metadata' "app/[locale]/about/page.tsx"
    </automated>
  </verify>
  <done>
    app/[locale]/page.tsx contains "title: { absolute:" string.
    app/[locale]/solutions/page.tsx contains "export const metadata".
    app/[locale]/work/page.tsx contains "export const metadata".
    app/[locale]/about/page.tsx contains "export const metadata".
    None of the four files contain an "openGraph" key in their metadata object.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add generateMetadata to solution detail page with slug-keyed lookup table</name>
  <files>
    app/[locale]/solutions/[slug]/page.tsx
  </files>
  <read_first>
    - app/[locale]/solutions/[slug]/page.tsx — read full file; note existing imports, generateStaticParams, dynamicParams, and default export structure
    - .planning/phases/04-launch-prep/04-RESEARCH.md — Pattern 4 (generateMetadata with await params, slugMeta lookup table), Pitfall 5 (params is a Promise — must await)
    - lib/solutions.ts — confirm the 4 slug values match the slugMeta keys
  </read_first>
  <action>
    Add "import type { Metadata } from 'next';" to the existing imports block (if not present).

    Add a const slugMeta lookup table above the generateMetadata function. The table must contain exactly these 4 entries with slug keys:
    - "fleet-mobility": title "Fleet Mobility", description "Real-time fleet tracking and dispatch for EV and mixed fleets. Built for operators managing high-utilisation vehicles at scale."
    - "iot-hardware": title "IoT Hardware", description "Industrial IoT devices and edge firmware for connected operations. Hardened for field deployment in transport and logistics."
    - "logistics-intelligence": title "Logistics Intelligence", description "Data pipelines and routing logic that turn raw fleet telemetry into operational decisions. Built for last-mile and mid-mile operations."
    - "ai-operations": title "AI Operations", description "Machine learning systems that surface anomalies, forecast demand, and automate triage for complex transport operations."

    Add the generateMetadata function. It must:
    - Accept params typed as Promise<{ locale: string; slug: string }>
    - Use "const { slug } = await params" (async access — required per Pitfall 5)
    - Look up slugMeta[slug]
    - Return { title: meta.title, description: meta.description } if found, or {} if not found
    - Return type is Promise<Metadata>
    - NOT set openGraph in the returned object

    Place the slugMeta const and generateMetadata function after imports and before generateStaticParams. Keep all existing code (generateStaticParams, dynamicParams, default export) exactly as-is.
  </action>
  <verify>
    <automated>
      grep -c 'export async function generateMetadata' "app/[locale]/solutions/[slug]/page.tsx" &&
      grep -c 'await params' "app/[locale]/solutions/[slug]/page.tsx" &&
      grep -c 'fleet-mobility' "app/[locale]/solutions/[slug]/page.tsx" &&
      npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    app/[locale]/solutions/[slug]/page.tsx contains "export async function generateMetadata".
    The file contains "await params" (async params destructuring).
    The file contains all 4 slug keys: fleet-mobility, iot-hardware, logistics-intelligence, ai-operations.
    The file does NOT contain an "openGraph" key in the generateMetadata return.
    npm run build exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| params.slug → slugMeta lookup | params.slug comes from the static route params defined in generateStaticParams; it is not user-supplied at runtime (static export) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | slugMeta description strings | accept | All strings are public marketing copy; no sensitive data |
| T-04-06 | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After all tasks complete:
- grep 'title.*absolute' "app/[locale]/page.tsx" returns a match
- grep 'export const metadata' "app/[locale]/solutions/page.tsx" returns a match
- grep 'export async function generateMetadata' "app/[locale]/solutions/[slug]/page.tsx" returns a match
- grep 'await params' "app/[locale]/solutions/[slug]/page.tsx" returns a match
- None of the 5 files contain an openGraph key in their metadata export/return
- npm run build exits 0
</verification>

<success_criteria>
All 5 page routes (home, solutions index, solution detail, work, about) have metadata exports. Home uses title.absolute. Solution detail uses async generateMetadata with slugMeta lookup and awaited params. No page-level metadata sets openGraph (preserving inherited OG image from root layout). Build passes.
</success_criteria>

<output>
Create .planning/phases/04-launch-prep/04-03-SUMMARY.md when done.
</output>
