---
phase: 04-launch-prep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
autonomous: true
requirements:
  - LAUNCH-02

must_haves:
  truths:
    - "Every page on the site resolves absolute OG image URL to https://sdt.technology/og.png"
    - "Twitter card type is summary_large_image on every page"
    - "Page titles that don't set their own title still show 'SDT tech — Infrastructure for intelligent operations.'"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Root metadata with metadataBase, openGraph defaults, twitter defaults, title template"
      contains: "metadataBase: new URL(\"https://sdt.technology\")"
  key_links:
    - from: "app/layout.tsx metadata.metadataBase"
      to: "public/og.png"
      via: "openGraph.images[0].url = /og.png"
      pattern: "metadataBase.*sdt\\.technology"
    - from: "app/layout.tsx metadata.title.template"
      to: "per-page metadata exports"
      via: "Next.js metadata merge"
      pattern: "template.*%s.*SDT tech"
---

<objective>
Add site-wide metadata defaults to app/layout.tsx: metadataBase, openGraph image defaults, twitter card type, and a title template that all child page metadata objects will inherit.

Purpose: The root layout metadata is the single place Next.js resolves absolute URLs and applies inherited OG/Twitter defaults. Setting these here means per-page metadata (Plan 04-03) only needs to set title and description — it inherits the OG image and twitter card automatically.

Output: app/layout.tsx with a complete Metadata export containing metadataBase, title.template, openGraph.siteName/images/locale/type, and twitter.card/images.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current app/layout.tsx metadata export (lines 12-15): -->
<!--   export const metadata: Metadata = { -->
<!--     title: `${siteConfig.name} tech`, -->
<!--     description: siteConfig.tagline, -->
<!--   }; -->
<!-- siteConfig.name = "SDT", siteConfig.tagline = "Infrastructure for intelligent operations." -->
<!-- Domain confirmed in STATE.md: sdt.technology (not sdttech.co) -->
<!-- og.png will be at /public/og.png (served as /og.png) — created by Plan 04-01 -->
<!-- CRITICAL PITFALL (from RESEARCH.md): Per-page metadata that sets openGraph.* at all will -->
<!--   replace the ENTIRE openGraph object (shallow merge). Per-page metadata must NOT set -->
<!--   openGraph — only title and description. Root layout openGraph is inherited by default. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace root layout metadata with full site-wide SEO defaults</name>
  <files>app/layout.tsx</files>
  <read_first>
    - app/layout.tsx — read current metadata export and all imports to understand what to preserve
    - config/site.ts — siteConfig.name ("SDT"), siteConfig.tagline — used in the default description
    - .planning/phases/04-launch-prep/04-RESEARCH.md — Pattern 1 (root layout metadata), Pattern 3 (home page absolute title note), Pitfall 1 (openGraph shallow merge)
    - .planning/phases/04-launch-prep/04-UI-SPEC.md — LAUNCH-02 metadata values (metadataBase, og.png dims, alt text)
  </read_first>
  <action>
    Replace the existing metadata export in app/layout.tsx. Keep all other content (imports, font setup, RootLayout component) exactly as-is. Only modify the metadata export.

    The new metadata export must contain:
    - metadataBase: new URL("https://sdt.technology")
    - title: { template: "%s — SDT tech", default: "SDT tech — Infrastructure for intelligent operations." }
    - description: "SDT tech builds hardware-aware fleet mobility, IoT, logistics, and AI operations systems for enterprise operators."
    - openGraph: { siteName: "SDT tech", images: [{ url: "/og.png", width: 1200, height: 630, alt: "SDT tech — Infrastructure for intelligent operations." }], locale: "en_US", type: "website" }
    - twitter: { card: "summary_large_image", images: ["/og.png"] }

    The siteConfig import is already present in the file but the new hardcoded description string replaces the tagline reference (the description is longer than siteConfig.tagline). Keep the siteConfig import because the RootLayout component may reference it elsewhere; do not remove it unless confirmed unused after reading the full file.

    Do NOT add metadataBase to app/[locale]/layout.tsx — it belongs only in the root layout per the research finding.
  </action>
  <verify>
    <automated>
      grep -c 'metadataBase: new URL' app/layout.tsx &&
      grep -c 'summary_large_image' app/layout.tsx &&
      grep -c 'template.*%s' app/layout.tsx &&
      npm run build 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    app/layout.tsx contains:
    - "metadataBase: new URL(\"https://sdt.technology\")" (exactly)
    - "summary_large_image" string
    - title.template containing "%s"
    - openGraph.images with url: "/og.png", width: 1200, height: 630
    - twitter.card: "summary_large_image"
    npm run build exits 0 with no TypeScript errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metadata → browser | Title and description are static strings; no user input surfaces here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | metadataBase URL hardcoded | accept | Domain is publicly known (sdt.technology); no secrets in metadata |
| T-04-04 | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After task completes:
- grep 'metadataBase' app/layout.tsx returns a match containing "sdt.technology"
- grep 'summary_large_image' app/layout.tsx returns a match
- grep 'template' app/layout.tsx returns a match containing "%s"
- npm run build exits 0
</verification>

<success_criteria>
app/layout.tsx metadata export contains metadataBase, title template, openGraph with /og.png image reference (1200×630), and twitter.card summary_large_image. Build passes. All child pages will inherit the OG image and twitter card without setting them explicitly.
</success_criteria>

<output>
Create .planning/phases/04-launch-prep/04-02-SUMMARY.md when done.
</output>
