---
phase: 03-interior-pages
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - messages/en.json
  - messages/id.json
  - messages/zh.json
  - components/PageHero.tsx
  - components/solutions/SolutionsGrid.tsx
  - app/[locale]/solutions/page.tsx
autonomous: true
requirements:
  - SOL-01

must_haves:
  truths:
    - "Visiting /en/solutions renders an eyebrow 'SOLUTIONS', H1 heading, and sub-line"
    - "The solutions grid shows all 4 solution cards, each is a full-card clickable link to /en/solutions/{slug}"
    - "Visiting /id/solutions and /zh/solutions renders the page in the correct language"
    - "PageHero component is available for reuse by work and about pages (Wave 2 peers)"
  artifacts:
    - path: "components/PageHero.tsx"
      provides: "Shared interior page hero (eyebrow + H1 + optional sub)"
      exports: ["PageHero"]
    - path: "components/solutions/SolutionsGrid.tsx"
      provides: "2-col grid of 4 linked SolutionCards"
      exports: ["SolutionsGrid"]
    - path: "app/[locale]/solutions/page.tsx"
      provides: "Solutions index page route"
      contains: "generateStaticParams"
  key_links:
    - from: "components/solutions/SolutionsGrid.tsx"
      to: "lib/solutions.ts"
      via: "import solutions array"
      pattern: "import.*solutions.*from.*@/lib/solutions"
    - from: "components/solutions/SolutionsGrid.tsx"
      to: "@/i18n/navigation"
      via: "locale-aware Link"
      pattern: "import.*Link.*from.*@/i18n/navigation"
    - from: "app/[locale]/solutions/page.tsx"
      to: "components/PageHero.tsx"
      via: "import PageHero"
      pattern: "import.*PageHero.*from.*@/components/PageHero"
---

<objective>
Build the solutions index page as a complete vertical slice: add translation keys to all 3 locale files, create PageHero (shared) and SolutionsGrid (solutions-specific) components, and assemble app/[locale]/solutions/page.tsx.

Purpose: A buyer visiting /[locale]/solutions sees all 4 solution cards and can navigate to any detail page.
Output: PageHero component, SolutionsGrid component, solutions/page.tsx, updated translation files in all 3 locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-interior-pages/03-CONTEXT.md
@.planning/phases/03-interior-pages/03-RESEARCH.md
@.planning/phases/03-interior-pages/03-PATTERNS.md
@.planning/phases/03-interior-pages/03-UI-SPEC.md

<interfaces>
<!-- Key patterns extracted from codebase -->

From components/home/HeroSection.tsx (PageHero analog — eyebrow/H1/sub structure):
  Section wrapper: <section className="bg-background py-24 md:py-32 px-6">
  Container: <div className="mx-auto max-w-content flex flex-col items-center text-center gap-4">
  Eyebrow: <p className="text-xs font-normal tracking-widest text-text-secondary uppercase">
  H1: <h1 className="text-5xl md:text-6xl font-bold tracking-tighter leading-none max-w-3xl">
  Sub: <p className="text-base font-normal leading-relaxed text-text-secondary max-w-xl">
  NOTE: PageHero has NO dot-grid, NO min-h-screen — those are home-hero-only.

From components/home/SolutionsStrip.tsx (SolutionsGrid analog):
  Section: <section className="py-24 md:py-32 px-6 bg-background">
  Grid: <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 lg:gap-6">
  Uses SolutionCard with named export from @/components/home/SolutionCard

From components/home/SolutionCard.tsx (props interface):
  Props: { icon: string; title: string; desc: string; tag: string }
  Card hover: "hover:border-cyber-jade transition-colors duration-150 ease-out"
  No href prop — wrap externally with <Link>

From i18n/navigation.ts:
  import { Link } from "@/i18n/navigation"  // ALWAYS — never next/link

From app/[locale]/page.tsx (page setup pattern):
  export function generateStaticParams() { return [{ locale: "en" }, { locale: "id" }, { locale: "zh" }] }
  export const dynamicParams = false
  async function Page({ params }: { params: Promise<{ locale: string }> }) {
    const { locale } = await params
    setRequestLocale(locale)
  }

From messages/en.json (existing solutions namespace to extend — DO NOT remove existing keys):
  "solutions": {
    "pageTitle": "Solutions",
    "card1Title": "Fleet & EV Mobility", "card1Desc": "...", "card1Tag": "Fleet",
    "card2Title": "IoT & Hardware", "card2Desc": "...", "card2Tag": "IoT",
    "card3Title": "Logistics Intelligence", "card3Desc": "...", "card3Tag": "Logistics",
    "card4Title": "AI Operations", "card4Desc": "...", "card4Tag": "AI"
  }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add solutions index translation keys to all 3 locale files</name>
  <files>messages/en.json, messages/id.json, messages/zh.json</files>
  <read_first>
    - messages/en.json (read entire file — understand existing structure before modifying)
    - messages/id.json (read entire file)
    - messages/zh.json (read entire file)
    - .planning/phases/03-interior-pages/03-CONTEXT.md (D-01: Claude drafts copy; D-03: outcome-first tone)
  </read_first>
  <action>
    Add the following new keys to the existing "solutions" namespace in all 3 locale files. Do NOT remove any existing keys (card1Title, card1Desc, etc.). Merge new keys alongside existing ones.

    New keys to add inside the "solutions" object:
    - "pageEyebrow": eyebrow label for solutions index hero (e.g., "SOLUTIONS")
    - "pageSub": one-line subheading beneath the H1 (e.g., "The software layer for real-world operations.")

    For en.json the solutions index sub-namespace keys follow the slugs from lib/solutions.ts. Add sub-objects under "solutions" for each slug. Each slug sub-object needs:
    - "title": solution name (e.g., "Fleet & EV Mobility")
    - "desc": one-line card description (same as existing card1Desc etc.)
    - "tag": the category tag (same as existing card1Tag etc.)

    Per D-01: Claude drafts copy from company context.
    Per D-03: outcome-first, no jargon — business operators are the audience.

    Slug-to-existing-key mapping (reuse the existing card copy — do not rewrite what Phase 2 already has):
    - fleet-mobility title = card1Title value, desc = card1Desc value, tag = card1Tag value
    - iot-hardware title = card2Title value, desc = card2Desc value, tag = card2Tag value
    - logistics-intelligence title = card3Title value, desc = card3Desc value, tag = card3Tag value
    - ai-operations title = card4Title value, desc = card4Desc value, tag = card4Tag value

    For id.json and zh.json: translate the new pageEyebrow and pageSub keys. For the slug sub-objects (title/desc/tag), translate all values from en.json following the machine-translation pattern already used in those files (consistent with Phase 2 translations).

    NOTE: Phase 3 detail keys (problemStatement, whoThisIsFor, capabilities) go in Plan 03-05, not here.
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/Website && node -e "const e=require('./messages/en.json');const i=require('./messages/id.json');const z=require('./messages/zh.json');console.log('en pageEyebrow:', e.solutions.pageEyebrow);console.log('en fleet-mobility title:', e.solutions['fleet-mobility']?.title);console.log('id pageEyebrow:', i.solutions.pageEyebrow);console.log('zh pageEyebrow:', z.solutions.pageEyebrow);"</automated>
  </verify>
  <acceptance_criteria>
    - messages/en.json solutions object contains "pageEyebrow" key with a non-empty string value
    - messages/en.json solutions object contains "pageSub" key with a non-empty string value
    - messages/en.json solutions object contains "fleet-mobility" sub-object with "title", "desc", "tag" keys
    - messages/en.json solutions object contains "iot-hardware" sub-object with "title", "desc", "tag" keys
    - messages/en.json solutions object contains "logistics-intelligence" sub-object with "title", "desc", "tag" keys
    - messages/en.json solutions object contains "ai-operations" sub-object with "title", "desc", "tag" keys
    - All existing card1Title, card2Title, card3Title, card4Title keys still present (not removed)
    - messages/id.json and messages/zh.json have matching structure with translated values
    - All 3 files are valid JSON (node -e "require('./messages/en.json')" exits 0)
  </acceptance_criteria>
  <done>All 3 locale files have solutions index keys; existing keys untouched; JSON is valid</done>
</task>

<task type="auto">
  <name>Task 2: Create PageHero component and SolutionsGrid component, assemble solutions/page.tsx</name>
  <files>
    components/PageHero.tsx,
    components/solutions/SolutionsGrid.tsx,
    app/[locale]/solutions/page.tsx
  </files>
  <read_first>
    - components/home/HeroSection.tsx (eyebrow/H1/sub pattern, section layout)
    - components/home/SolutionsStrip.tsx (grid layout, SolutionCard usage, getTranslations pattern)
    - components/home/SolutionCard.tsx (props interface: icon/title/desc/tag; no href)
    - components/home/FooterCTA.tsx (reuse on solutions page — no changes needed)
    - app/[locale]/page.tsx (generateStaticParams, dynamicParams, page setup pattern)
    - lib/solutions.ts (solution array to iterate — created in Plan 03-01)
    - messages/en.json (confirm new keys from Task 1 are present before referencing them)
    - .planning/phases/03-interior-pages/03-UI-SPEC.md (layout contracts, spacing, type scale)
  </read_first>
  <action>
    Create 3 new files. Follow each file's spec precisely:

    FILE 1: components/PageHero.tsx
    - Named export: export function PageHero({ eyebrow, heading, sub }: PageHeroProps)
    - Interface PageHeroProps: { eyebrow: string; heading: string; sub?: string }
    - PageHero is a synchronous (not async) component — it receives pre-translated strings as props
    - Section wrapper: className="bg-background py-24 md:py-32 px-6"
    - Container: className="mx-auto max-w-content flex flex-col items-center text-center gap-4"
    - Eyebrow: <p className="text-xs font-normal tracking-widest text-text-secondary uppercase">{eyebrow}</p>
    - H1: <h1 className="text-5xl md:text-6xl font-bold tracking-tighter leading-none max-w-3xl">{heading}</h1>
    - Sub (conditional): <p className="text-base font-normal leading-relaxed text-text-secondary max-w-xl">{sub}</p> — render only if sub is provided
    - No dot-grid, no min-h-screen, no CTA button in this component

    FILE 2: components/solutions/SolutionsGrid.tsx
    - Create the components/solutions/ directory
    - Async server component: export async function SolutionsGrid()
    - Imports: getTranslations from "next-intl/server", Link from "@/i18n/navigation", solutions from "@/lib/solutions", SolutionCard from "@/components/home/SolutionCard"
    - Call: const st = await getTranslations("solutions")
    - Section wrapper: className="py-24 md:py-32 px-6 bg-background"
    - Container: className="mx-auto max-w-content"
    - Grid: className="grid grid-cols-1 sm:grid-cols-2 gap-4"  (2-col on sm+; solutions index is 2-col not 4-col per UI-SPEC)
    - For each solution in solutions array, render:
      <Link href={`/solutions/${solution.slug}`} className="block">
        <SolutionCard
          icon={solution.icon}
          title={st(`${solution.slug}.title`)}
          desc={st(`${solution.slug}.desc`)}
          tag={st(`${solution.slug}.tag`)}
        />
      </Link>
    - Use solution.slug as the React key on the Link

    FILE 3: app/[locale]/solutions/page.tsx
    - Create the app/[locale]/solutions/ directory
    - Imports: setRequestLocale, getTranslations from "next-intl/server"; PageHero from "@/components/PageHero"; SolutionsGrid from "@/components/solutions/SolutionsGrid"; FooterCTA from "@/components/home/FooterCTA"
    - export function generateStaticParams() returning [{ locale: "en" }, { locale: "id" }, { locale: "zh" }]
    - export const dynamicParams = false
    - Default async export: export default async function SolutionsPage({ params }: { params: Promise<{ locale: string }> })
    - await params to get locale; call setRequestLocale(locale) before any other next-intl call
    - const st = await getTranslations("solutions")
    - Return JSX: <PageHero eyebrow={st("pageEyebrow")} heading={st("pageTitle")} sub={st("pageSub")} /> then <SolutionsGrid /> then <FooterCTA />
    - No Volt Green on this page — FooterCTA already uses cyber-jade outline button per UI-SPEC
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/Website && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - components/PageHero.tsx exists with named export PageHero
    - PageHero accepts eyebrow (string), heading (string), sub (optional string) props
    - PageHero uses className "text-5xl md:text-6xl font-bold tracking-tighter leading-none" on the h1
    - components/solutions/SolutionsGrid.tsx exists with named export SolutionsGrid
    - SolutionsGrid imports Link from "@/i18n/navigation" (not "next/link")
    - SolutionsGrid iterates solutions from "@/lib/solutions"
    - SolutionsGrid wraps each SolutionCard in a <Link href="/solutions/{slug}">
    - app/[locale]/solutions/page.tsx exists with default export SolutionsPage
    - page.tsx contains generateStaticParams returning 3 locale objects
    - page.tsx contains dynamicParams = false
    - page.tsx calls setRequestLocale(locale) before getTranslations
    - npm run build exits 0 and emits /en/solutions, /id/solutions, /zh/solutions static pages
  </acceptance_criteria>
  <done>Solutions index page builds and renders at /en/solutions with 4 linked cards and correct hero</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client browser → static HTML | All content is build-time static. No user input. Mailto link uses siteConfig.email. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Information Disclosure | mailto link in FooterCTA | accept | Email is intentionally public for lead generation; siteConfig.email is already the established pattern |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
Build passes:
  cd /Users/shannendorothee/Projects/Website && npm run build

Static pages emitted (check build output):
  /en/solutions, /id/solutions, /zh/solutions

Translation keys present:
  node -e "const e=require('./messages/en.json');console.log(e.solutions.pageEyebrow, e.solutions['fleet-mobility'].title)"

SolutionsGrid uses locale-aware Link:
  grep -r "from.*@/i18n/navigation" components/solutions/SolutionsGrid.tsx
</verification>

<success_criteria>
npm run build exits 0; /[locale]/solutions resolves in all 3 locales; 4 linked solution cards render; PageHero and SolutionsGrid exist as named-exported components.
</success_criteria>

<output>
Create .planning/phases/03-interior-pages/03-02-SUMMARY.md when done
</output>
