---
phase: 03-interior-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/solutions.ts
autonomous: true
requirements:
  - SOL-02

must_haves:
  truths:
    - "lib/solutions.ts exports a typed Solution[] array with 4 entries"
    - "Slugs match exactly: fleet-mobility, iot-hardware, logistics-intelligence, ai-operations"
    - "fleet-mobility has status 'live'; the other three have status 'in-development'"
    - "Icons match SolutionsStrip: ⚡ ◈ ⊞ ∿ in that order"
  artifacts:
    - path: "lib/solutions.ts"
      provides: "Single source of truth for solution metadata"
      exports: ["Solution", "SolutionStatus", "solutions"]
      contains: "fleet-mobility"
  key_links:
    - from: "lib/solutions.ts"
      to: "app/[locale]/solutions/[slug]/page.tsx"
      via: "generateStaticParams import"
      pattern: "import.*solutions.*from.*@/lib/solutions"
    - from: "lib/solutions.ts"
      to: "app/[locale]/solutions/page.tsx"
      via: "SolutionsGrid import"
      pattern: "solutions.*from.*@/lib/solutions"
---

<objective>
Create lib/solutions.ts — the typed TypeScript data file holding solution metadata (slug, status, icon, display order). This is the shared dependency required by all solutions pages in Wave 2 and Wave 3.

Purpose: generateStaticParams in the [slug] detail page reads from this file. SolutionsGrid on the index page reads from it too. Creating it first in Wave 1 eliminates any dependency conflict.
Output: lib/solutions.ts with exported Solution interface, SolutionStatus type, and solutions const array.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-interior-pages/03-CONTEXT.md
@.planning/phases/03-interior-pages/03-RESEARCH.md
@.planning/phases/03-interior-pages/03-PATTERNS.md

<interfaces>
<!-- Analogs to follow — extracted from codebase -->

From i18n/routing.ts (typed const export pattern):
  export const routing = defineRouting({ locales: ["en", "id", "zh"], defaultLocale: "en" })

From components/home/SolutionsStrip.tsx (icon values to reuse):
  { icon: "⚡", ... }  // fleet-mobility — order 1
  { icon: "◈",  ... }  // iot-hardware — order 2
  { icon: "⊞",  ... }  // logistics-intelligence — order 3
  { icon: "∿",  ... }  // ai-operations — order 4
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/solutions.ts data file</name>
  <files>lib/solutions.ts</files>
  <read_first>
    - components/home/SolutionsStrip.tsx (icon values: ⚡ ◈ ⊞ ∿ in display order)
    - i18n/routing.ts (typed const export pattern to follow)
    - .planning/phases/03-interior-pages/03-CONTEXT.md (D-02: fleet-mobility = "live"; others = "in-development"; D-06: this file holds slug + status + icon + order only)
  </read_first>
  <action>
    Create lib/solutions.ts as a new file at the project root lib/ directory (same level as i18n/).

    Export three items:
    1. Type alias SolutionStatus = "live" | "in-development"
    2. Interface Solution with fields: slug (string), status (SolutionStatus), icon (string), order (number)
    3. Const array solutions: Solution[] with exactly 4 entries in this order:
       - slug: "fleet-mobility", status: "live", icon: "⚡", order: 1
       - slug: "iot-hardware", status: "in-development", icon: "◈", order: 2
       - slug: "logistics-intelligence", status: "in-development", icon: "⊞", order: 3
       - slug: "ai-operations", status: "in-development", icon: "∿", order: 4

    Per D-06: this file holds metadata only. No text content (titles, descriptions) — those stay in messages/*.json.
    Per D-02: fleet-mobility is "live"; the other three are "in-development".

    Use named exports (not default export). No imports needed — this file has no dependencies.
    No @/ alias imports are needed since this file has no external dependencies.
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/Website && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - lib/solutions.ts exists at the project root
    - File exports three named items: SolutionStatus (type), Solution (interface), solutions (const array)
    - solutions array has exactly 4 entries
    - solutions[0].slug === "fleet-mobility" and solutions[0].status === "live"
    - solutions[1].slug === "iot-hardware" and solutions[1].status === "in-development"
    - solutions[2].slug === "logistics-intelligence" and solutions[2].status === "in-development"
    - solutions[3].slug === "ai-operations" and solutions[3].status === "in-development"
    - Icons in order: ⚡ ◈ ⊞ ∿
    - TypeScript compiles without errors: npx tsc --noEmit exits 0
    - No default export — only named exports
  </acceptance_criteria>
  <done>lib/solutions.ts exists, exports are correct, TypeScript compiles clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time data | lib/solutions.ts is read at build time by generateStaticParams — no runtime trust boundary. Static data file, no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | lib/solutions.ts slug values | accept | Slugs are build-time constants; mismatched slugs produce 404s at build not runtime. TypeScript type safety catches structural errors. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
TypeScript compiles clean:
  cd /Users/shannendorothee/Projects/Website && npx tsc --noEmit

File content check:
  grep -c "fleet-mobility\|iot-hardware\|logistics-intelligence\|ai-operations" lib/solutions.ts
  Expected: 4 (one per slug)
</verification>

<success_criteria>
lib/solutions.ts exists with 4 Solution entries, correct slugs, correct status values, correct icons, TypeScript compiles without errors.
</success_criteria>

<output>
Create .planning/phases/03-interior-pages/03-01-SUMMARY.md when done
</output>
