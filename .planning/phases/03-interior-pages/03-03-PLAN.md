---
phase: 03-interior-pages
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - messages/en.json
  - messages/id.json
  - messages/zh.json
  - components/work/WorkCard.tsx
  - components/work/GhostCard.tsx
  - app/[locale]/work/page.tsx
autonomous: true
requirements:
  - WORK-01
  - WORK-02

must_haves:
  truths:
    - "Visiting /en/work renders a PageHero with 'Work' eyebrow and heading"
    - "EV Fleet Operations WorkCard shows status 'Live' badge (ev-teal), tags Fleet/EV/Operations, and expanded 2-3 sentence description"
    - "Two GhostCard placeholders render alongside WorkCard with dashed borders and 'Coming soon' text"
    - "Visiting /id/work and /zh/work renders the page in the correct language"
  artifacts:
    - path: "components/work/WorkCard.tsx"
      provides: "EV Fleet real project card with status badge + tags + expanded description"
      exports: ["WorkCard"]
    - path: "components/work/GhostCard.tsx"
      provides: "Dashed-border coming-soon placeholder card"
      exports: ["GhostCard"]
    - path: "app/[locale]/work/page.tsx"
      provides: "Work page route"
      contains: "generateStaticParams"
  key_links:
    - from: "app/[locale]/work/page.tsx"
      to: "components/PageHero.tsx"
      via: "import PageHero"
      pattern: "import.*PageHero.*from.*@/components/PageHero"
    - from: "components/work/WorkCard.tsx"
      to: "messages/en.json"
      via: "getTranslations('work')"
      pattern: "getTranslations.*work"
---

<objective>
Build the work page as a complete vertical slice: add work page translation keys to all 3 locale files, create WorkCard (real project card) and GhostCard (placeholder) components, and assemble app/[locale]/work/page.tsx.

Purpose: A buyer visiting /[locale]/work sees the live EV Fleet project plus two ghost cards signalling more projects are coming.
Output: WorkCard, GhostCard components, work/page.tsx, updated translation files in all 3 locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-interior-pages/03-CONTEXT.md
@.planning/phases/03-interior-pages/03-RESEARCH.md
@.planning/phases/03-interior-pages/03-PATTERNS.md
@.planning/phases/03-interior-pages/03-UI-SPEC.md

<interfaces>
<!-- Key patterns extracted from codebase -->

From components/home/WorkTeaser.tsx (WorkCard analog — status badge, card shell):
  Card shell: className="bg-surface border border-border-subtle rounded-lg p-6 flex flex-col gap-4"
  Live badge: <span className="self-start text-xs font-normal tracking-widest uppercase px-2 py-1 rounded bg-ev-teal/10 text-ev-teal">
  Card title: text-base font-bold (NOT text-lg — UI-SPEC mandates text-base for new Phase 3 components)
  Description: <p className="text-base font-normal leading-relaxed text-text-secondary">

From components/home/SolutionCard.tsx (tag pill pattern):
  Tag: <span className="self-start text-xs font-medium bg-grid-violet/10 text-grid-violet px-2 py-1 rounded">

Card hover (from SolutionCard.tsx):
  "hover:border-cyber-jade transition-colors duration-150 ease-out"

From messages/en.json (existing work keys to preserve and extend):
  "work": {
    "pageTitle": "Work",
    "evFleetTitle": "EV Fleet Operations",
    "evFleetDesc": "End-to-end operations platform for an electric vehicle fleet — real-time tracking, charging management, and dispatch."
  }

From app/[locale]/page.tsx (page setup pattern):
  await params, setRequestLocale(locale), generateStaticParams for 3 locales, dynamicParams = false

PageHero is created in Plan 03-02, available at @/components/PageHero
  Named export: PageHero({ eyebrow, heading, sub? })

Tailwind token reference for ghost cards:
  border-border-subtle = #2a2a2a (from tailwind.config.ts)
  text-text-muted = #52525b (from tailwind.config.ts)
  bg-surface = #111111 (from tailwind.config.ts)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add work page translation keys to all 3 locale files</name>
  <files>messages/en.json, messages/id.json, messages/zh.json</files>
  <read_first>
    - messages/en.json (read entire file — work namespace currently has 3 keys; add new ones without removing them)
    - messages/id.json (read entire file)
    - messages/zh.json (read entire file)
    - .planning/phases/03-interior-pages/03-CONTEXT.md (D-08: tags Fleet/EV/Operations; D-09: expanded description 2-3 sentences using evFleetDesc as opening)
  </read_first>
  <action>
    Add new keys to the existing "work" namespace in all 3 locale files. Do NOT remove existing keys (pageTitle, evFleetTitle, evFleetDesc).

    New keys to add inside the "work" object for en.json:
    - "pageEyebrow": "WORK"
    - "pageSub": one-line page subheading (e.g., "One project in production. More on the way.")
    - "evFleetExpandedDesc": 1-2 additional sentences expanding on evFleetDesc. Use the existing evFleetDesc value as the opening sentence, then add sentences about scale or integration — e.g., mention GPS telemetry, charging station APIs, or depot coverage. Example tone (D-09, D-03): "Connects GPS telemetry from 40+ vehicles with depot charging APIs to surface real-time state across every operator screen."
    - "evFleetTag1": "Fleet"
    - "evFleetTag2": "EV"
    - "evFleetTag3": "Operations"

    Per D-08: tags are exactly Fleet, EV, Operations — these are the grid-violet category tags.
    Per D-09: evFleetDesc is the opener; evFleetExpandedDesc provides 1-2 more sentences about scale/integration.

    For id.json and zh.json: translate all new keys following the machine-translation pattern already used in those files.
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/Website && node -e "const e=require('./messages/en.json');console.log('pageEyebrow:', e.work.pageEyebrow,'tag1:',e.work.evFleetTag1,'expandedDesc present:', !!e.work.evFleetExpandedDesc);"</automated>
  </verify>
  <acceptance_criteria>
    - messages/en.json work object contains "pageEyebrow" with value "WORK"
    - messages/en.json work object contains "pageSub" with a non-empty string
    - messages/en.json work object contains "evFleetExpandedDesc" with a non-empty string (1-2 sentences about scale/integration)
    - messages/en.json work object contains "evFleetTag1" = "Fleet", "evFleetTag2" = "EV", "evFleetTag3" = "Operations"
    - Existing keys "pageTitle", "evFleetTitle", "evFleetDesc" still present (not removed)
    - messages/id.json and messages/zh.json have matching structure with translated values
    - All 3 files remain valid JSON
  </acceptance_criteria>
  <done>Work page translation keys added to all 3 locale files; existing keys preserved; JSON valid</done>
</task>

<task type="auto">
  <name>Task 2: Create WorkCard, GhostCard components and assemble work/page.tsx</name>
  <files>
    components/work/WorkCard.tsx,
    components/work/GhostCard.tsx,
    app/[locale]/work/page.tsx
  </files>
  <read_first>
    - components/home/WorkTeaser.tsx (WorkCard analog — card shell, status badge, description pattern)
    - components/home/SolutionCard.tsx (tag pill pattern: bg-grid-violet/10 text-grid-violet)
    - components/PageHero.tsx (created in Plan 03-02 — read to confirm props interface)
    - app/[locale]/page.tsx (page setup pattern)
    - messages/en.json (confirm work keys from Task 1 before referencing)
    - .planning/phases/03-interior-pages/03-UI-SPEC.md (work page layout: 3-col lg, 2-col md, 1-col mobile)
  </read_first>
  <action>
    Create 3 new files:

    FILE 1: components/work/WorkCard.tsx
    - Create the components/work/ directory
    - Async server component: export async function WorkCard()
    - Imports: getTranslations from "next-intl/server"
    - const wt = await getTranslations("work")
    - Card shell: className="bg-surface border border-border-subtle rounded-lg p-6 flex flex-col gap-4 hover:border-cyber-jade transition-colors duration-150 ease-out"
    - Render in this order:
      1. Status badge (Live): <span className="self-start text-xs font-normal tracking-widest uppercase px-2 py-1 rounded bg-ev-teal/10 text-ev-teal">{wt("evFleetTitle")} ... </span>
         Actually render a common.status.live key: import getTranslations separately for "common" or use the wt for evFleetTitle.
         CORRECTION: Use <span className="self-start text-xs font-normal tracking-widest uppercase px-2 py-1 rounded bg-ev-teal/10 text-ev-teal"> with content from common.status.live. Import getTranslations twice: const wt = await getTranslations("work"); const ct = await getTranslations("common");  Then ct("status.live").
      2. Title: <h3 className="text-base font-bold">{wt("evFleetTitle")}</h3>
      3. Tags row: flex gap-2, 3 tags using evFleetTag1/2/3 with className="text-xs font-normal bg-grid-violet/10 text-grid-violet px-2 py-1 rounded"
      4. Description: <p className="text-base font-normal leading-relaxed text-text-secondary">{wt("evFleetDesc")}</p>
      5. Expanded description: <p className="text-base font-normal leading-relaxed text-text-secondary">{wt("evFleetExpandedDesc")}</p>

    FILE 2: components/work/GhostCard.tsx
    - Synchronous component: export function GhostCard()
    - No imports needed (no translations — "Coming soon" is static per CONTEXT.md)
    - Card: className="border border-dashed border-border-subtle bg-surface rounded-lg p-6 flex items-center justify-center cursor-default min-h-[200px]"
    - Content: <span className="text-sm font-normal text-text-muted">Coming soon</span>
    - No hover state, cursor-default

    FILE 3: app/[locale]/work/page.tsx
    - Create the app/[locale]/work/ directory
    - Imports: setRequestLocale, getTranslations from "next-intl/server"; PageHero from "@/components/PageHero"; WorkCard from "@/components/work/WorkCard"; GhostCard from "@/components/work/GhostCard"
    - export function generateStaticParams() returning [{ locale: "en" }, { locale: "id" }, { locale: "zh" }]
    - export const dynamicParams = false
    - Default async export: export default async function WorkPage({ params }: { params: Promise<{ locale: string }> })
    - await params to get locale; call setRequestLocale(locale) before getTranslations
    - const wt = await getTranslations("work")
    - Return JSX:
      1. PageHero with eyebrow={wt("pageEyebrow")} heading={wt("pageTitle")} sub={wt("pageSub")}
      2. A section wrapper with py-24 md:py-32 px-6 bg-background containing:
         - Container: mx-auto max-w-content
         - Grid: className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6"
         - <WorkCard />
         - <GhostCard />
         - <GhostCard />
    - No Volt Green on work page — FooterCTA is NOT added here (UI-SPEC shows work page ends with the cards grid)
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/Website && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - components/work/WorkCard.tsx exists with named export WorkCard
    - WorkCard card title uses "text-base font-bold" (not text-lg)
    - WorkCard renders 3 grid-violet tag pills for Fleet, EV, Operations
    - WorkCard renders the Live status badge with className including "bg-ev-teal/10 text-ev-teal"
    - components/work/GhostCard.tsx exists with named export GhostCard
    - GhostCard className includes "border-dashed" and "cursor-default" and "min-h-[200px]"
    - GhostCard contains static text "Coming soon" (not a translation call)
    - app/[locale]/work/page.tsx exists with default export WorkPage
    - work/page.tsx contains generateStaticParams returning 3 locale objects
    - work/page.tsx calls setRequestLocale(locale) before getTranslations
    - npm run build exits 0 and emits /en/work, /id/work, /zh/work static pages
  </acceptance_criteria>
  <done>Work page builds and renders at /en/work with EV Fleet card plus two ghost cards</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client browser → static HTML | All content is build-time static. No user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | Static work page content | accept | Content is intentionally public marketing copy; no sensitive data. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
Build passes:
  cd /Users/shannendorothee/Projects/Website && npm run build

Static pages emitted (check build output):
  /en/work, /id/work, /zh/work

Ghost card check:
  grep "border-dashed" components/work/GhostCard.tsx

WorkCard uses ev-teal badge:
  grep "bg-ev-teal/10" components/work/WorkCard.tsx
</verification>

<success_criteria>
npm run build exits 0; /[locale]/work resolves in all 3 locales; EV Fleet card renders with Live badge, 3 tags, expanded description; 2 GhostCards render with dashed borders.
</success_criteria>

<output>
Create .planning/phases/03-interior-pages/03-03-SUMMARY.md when done
</output>
