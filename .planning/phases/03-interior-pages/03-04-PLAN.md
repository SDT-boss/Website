---
phase: 03-interior-pages
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - messages/en.json
  - messages/id.json
  - messages/zh.json
  - components/about/BeliefGrid.tsx
  - components/about/CurrentFocusParagraph.tsx
  - app/[locale]/about/page.tsx
autonomous: true
requirements:
  - ABOUT-01
  - ABOUT-02

must_haves:
  truths:
    - "Visiting /en/about opens with a belief-statement H1 (not a founding story)"
    - "The BeliefGrid renders 3 belief columns with numbered cyber-jade/20 accents and text-base font-bold titles"
    - "A CurrentFocusParagraph renders below BeliefGrid with honest active-work tone"
    - "The About page ends with a FooterCTA using cyber-jade outline button (no Volt Green)"
    - "Visiting /id/about and /zh/about renders the page in the correct language"
  artifacts:
    - path: "components/about/BeliefGrid.tsx"
      provides: "3-column numbered belief statements"
      exports: ["BeliefGrid"]
    - path: "components/about/CurrentFocusParagraph.tsx"
      provides: "Current focus paragraph block"
      exports: ["CurrentFocusParagraph"]
    - path: "app/[locale]/about/page.tsx"
      provides: "About page route"
      contains: "generateStaticParams"
  key_links:
    - from: "app/[locale]/about/page.tsx"
      to: "components/home/FooterCTA.tsx"
      via: "ABOUT-02 — cyber-jade outline CTA"
      pattern: "import.*FooterCTA.*from.*@/components/home/FooterCTA"
    - from: "components/about/BeliefGrid.tsx"
      to: "messages/en.json"
      via: "getTranslations('about')"
      pattern: "getTranslations.*about"
---

<objective>
Build the about page as a complete vertical slice: add about page translation keys (including Claude-drafted copy) to all 3 locale files, create BeliefGrid and CurrentFocusParagraph components, and assemble app/[locale]/about/page.tsx with FooterCTA reuse.

Purpose: A buyer visiting /[locale]/about encounters a belief-led company narrative, 3 founding principles, current focus, and a cyber-jade contact CTA.
Output: BeliefGrid, CurrentFocusParagraph components, about/page.tsx, updated translation files in all 3 locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-interior-pages/03-CONTEXT.md
@.planning/phases/03-interior-pages/03-RESEARCH.md
@.planning/phases/03-interior-pages/03-PATTERNS.md
@.planning/phases/03-interior-pages/03-UI-SPEC.md

<interfaces>
<!-- Key patterns extracted from codebase -->

From components/home/ApproachSection.tsx (BeliefGrid analog — 3-col numbered items):
  Grid: <div className="flex flex-col gap-8 md:grid md:grid-cols-3 md:gap-8">
  Number accent: <span className="text-4xl font-bold text-cyber-jade/20 leading-none">{item.num}</span>
  H3: className="text-base font-bold" (NOT text-lg — UI-SPEC mandates text-base for new Phase 3 components)
  Body: className="text-base font-normal leading-relaxed text-text-secondary"
  const beliefs array above component: [{ num: "01", titleKey: "belief1Title", bodyKey: "belief1Body" }, ...]

From components/home/WorkTeaser.tsx (CurrentFocusParagraph analog — prose paragraph block):
  Section: <section className="py-24 md:py-32 px-6 bg-background">
  Container: <div className="mx-auto max-w-content">
  Paragraph: <p className="text-base font-normal leading-relaxed text-text-secondary">

From components/home/FooterCTA.tsx (reuse on about page — no changes to this component):
  Already implements cyber-jade outline button. Drop in as-is.

From app/[locale]/page.tsx (page setup pattern):
  await params, setRequestLocale(locale), generateStaticParams, dynamicParams = false

PageHero available at @/components/PageHero (created in Plan 03-02)

From DESIGN.md (voice rules for belief statements):
  "Infrastructure should outlast the problem it solves." — observation-as-statement format
  Short. Direct. No adjective bloat. Headlines noun-first, present tense.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add about page translation keys to all 3 locale files (including Claude-drafted copy)</name>
  <files>messages/en.json, messages/id.json, messages/zh.json</files>
  <read_first>
    - messages/en.json (read entire file — about namespace currently has only pageTitle; add new keys)
    - messages/id.json (read entire file)
    - messages/zh.json (read entire file)
    - .planning/PROJECT.md (company identity, active domain, brand voice)
    - .planning/phases/03-interior-pages/03-CONTEXT.md (D-04: observation-as-statement H1; D-05: current focus tone — active fleet/EV production work + growing carefully; no aspirational vision-speak)
    - DESIGN.md (voice rules: short, direct, noun-first, present tense)
  </read_first>
  <action>
    Add new keys to the existing "about" namespace in all 3 locale files. Do NOT remove existing "pageTitle" key.

    New keys to add inside the "about" object for en.json (Claude drafts all copy per D-04):

    - "pageEyebrow": "ABOUT"
    - "beliefH1": A single-sentence belief statement in observation-as-statement format. Must NOT reference founding story, team size, or years. Examples of correct tone: "Infrastructure should outlast the problem it solves." / "Real operations can't wait for the demo environment." Draft a statement that reflects PT SDT Tech's core view — building serious, production-grade operations software for fleet, logistics, and industrial domains.
    - "beliefSub": One-line sub under the H1 that reinforces the belief without restating it. Example: "We build for operators who can't afford downtime."
    - "belief1Title": Title for belief statement 1 (text-base font-bold heading). Topics to cover across 3 beliefs: (1) hardware-aware / built for real conditions, (2) data as the foundation of every decision, (3) production systems that don't need a rewrite to scale. Draft each title as a short, noun-first statement.
    - "belief1Body": 2-3 sentence body for belief 1. Outcome-focused, operator-credible tone per D-03.
    - "belief2Title": Title for belief 2 (see topics above)
    - "belief2Body": 2-3 sentence body for belief 2
    - "belief3Title": Title for belief 3 (see topics above)
    - "belief3Body": 2-3 sentence body for belief 3
    - "currentFocusHeading": Short label for the current focus section (e.g., "Current Focus" or "Where we are now")
    - "currentFocus": 2-3 sentences in the tone specified by D-05: active fleet/EV production work + growing the platform carefully. Example: "We are deep in production on EV fleet operations — real vehicles, real depots, real dispatch. We are growing the platform deliberately, one solved problem at a time. IoT, logistics intelligence, and AI operations are next." Honest, operator-credible. No aspirational vision-speak.

    For id.json and zh.json: translate all new keys following the machine-translation pattern already used in those files. Existing "pageTitle" stays untouched.
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/Website && node -e "const e=require('./messages/en.json');console.log('beliefH1:', e.about.beliefH1,'belief1Title:', e.about.belief1Title,'currentFocus:', !!e.about.currentFocus);"</automated>
  </verify>
  <acceptance_criteria>
    - messages/en.json about object contains "pageEyebrow" = "ABOUT"
    - messages/en.json about object contains "beliefH1" — non-empty observation-as-statement (no founding story, no team size, no year)
    - messages/en.json about object contains "beliefSub" — non-empty string
    - messages/en.json about object contains "belief1Title", "belief1Body", "belief2Title", "belief2Body", "belief3Title", "belief3Body" — all non-empty
    - messages/en.json about object contains "currentFocusHeading" and "currentFocus" — currentFocus is 2-3 sentences about active fleet/EV work
    - beliefH1 value does NOT contain words "founded", "established", "2020", "2021", "2022", "2023", "team of"
    - Existing "pageTitle" key still present
    - messages/id.json and messages/zh.json have matching structure with translated values
    - All 3 files remain valid JSON
  </acceptance_criteria>
  <done>About page translation keys added to all 3 locale files; belief copy drafted per D-04/D-05; JSON valid</done>
</task>

<task type="auto">
  <name>Task 2: Create BeliefGrid, CurrentFocusParagraph components and assemble about/page.tsx</name>
  <files>
    components/about/BeliefGrid.tsx,
    components/about/CurrentFocusParagraph.tsx,
    app/[locale]/about/page.tsx
  </files>
  <read_first>
    - components/home/ApproachSection.tsx (BeliefGrid analog — 3-col grid, numbered accent, const beliefs array pattern)
    - components/home/WorkTeaser.tsx (CurrentFocusParagraph analog — section + prose block)
    - components/home/FooterCTA.tsx (reused on about page — confirm it has no dependencies that conflict)
    - components/PageHero.tsx (created in Plan 03-02 — confirm props interface)
    - app/[locale]/page.tsx (page setup pattern)
    - messages/en.json (confirm about keys from Task 1 before referencing)
    - .planning/phases/03-interior-pages/03-UI-SPEC.md (about page layout contract)
  </read_first>
  <action>
    Create 3 new files:

    FILE 1: components/about/BeliefGrid.tsx
    - Create the components/about/ directory
    - Async server component: export async function BeliefGrid()
    - Imports: getTranslations from "next-intl/server"
    - const at = await getTranslations("about")
    - Const beliefs array above the component function (static — same pattern as ApproachSection items):
      [{ num: "01", titleKey: "belief1Title", bodyKey: "belief1Body" }, { num: "02", ... }, { num: "03", ... }]
    - Section wrapper: className="py-24 md:py-32 px-6 bg-background"
    - Container: className="mx-auto max-w-content"
    - Grid: className="flex flex-col gap-8 md:grid md:grid-cols-3 md:gap-8"
    - Each belief cell: <div key={belief.num} className="flex flex-col gap-3">
    - Number accent: <span className="text-4xl font-bold text-cyber-jade/20 leading-none">{belief.num}</span>
    - Title: <h3 className="text-base font-bold">{at(belief.titleKey)}</h3>
    - Body: <p className="text-base font-normal leading-relaxed text-text-secondary">{at(belief.bodyKey)}</p>

    FILE 2: components/about/CurrentFocusParagraph.tsx
    - Async server component: export async function CurrentFocusParagraph()
    - Imports: getTranslations from "next-intl/server"
    - const at = await getTranslations("about")
    - Section wrapper: className="py-24 md:py-32 px-6 bg-surface"  (bg-surface for visual separation from BeliefGrid)
    - Container: className="mx-auto max-w-content"
    - Optional section label: <p className="text-xs font-normal tracking-widest uppercase text-text-secondary mb-6">{at("currentFocusHeading")}</p>
    - Paragraph: <p className="text-base font-normal leading-relaxed text-text-secondary max-w-2xl">{at("currentFocus")}</p>

    FILE 3: app/[locale]/about/page.tsx
    - Create the app/[locale]/about/ directory
    - Imports: setRequestLocale from "next-intl/server"; PageHero from "@/components/PageHero"; BeliefGrid from "@/components/about/BeliefGrid"; CurrentFocusParagraph from "@/components/about/CurrentFocusParagraph"; FooterCTA from "@/components/home/FooterCTA"; getTranslations from "next-intl/server"
    - export function generateStaticParams() returning [{ locale: "en" }, { locale: "id" }, { locale: "zh" }]
    - export const dynamicParams = false
    - Default async export: export default async function AboutPage({ params }: { params: Promise<{ locale: string }> })
    - await params to get locale; call setRequestLocale(locale) before getTranslations
    - const at = await getTranslations("about")
    - Return JSX in this order:
      1. PageHero with eyebrow={at("pageEyebrow")} heading={at("beliefH1")} sub={at("beliefSub")}
      2. BeliefGrid
      3. CurrentFocusParagraph
      4. FooterCTA
    - No Volt Green on this page — FooterCTA already uses cyber-jade outline button per ABOUT-02
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/Website && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - components/about/BeliefGrid.tsx exists with named export BeliefGrid
    - BeliefGrid has a const beliefs array with 3 entries (num: "01", "02", "03")
    - BeliefGrid H3 titles use className "text-base font-bold"
    - BeliefGrid number accents use className "text-cyber-jade/20"
    - components/about/CurrentFocusParagraph.tsx exists with named export CurrentFocusParagraph
    - app/[locale]/about/page.tsx exists with default export AboutPage
    - about/page.tsx imports FooterCTA from "@/components/home/FooterCTA"
    - about/page.tsx contains generateStaticParams returning 3 locale objects
    - about/page.tsx calls setRequestLocale(locale) before getTranslations
    - npm run build exits 0 and emits /en/about, /id/about, /zh/about static pages
  </acceptance_criteria>
  <done>About page builds and renders at /en/about with belief H1, 3-col BeliefGrid, CurrentFocusParagraph, FooterCTA</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client browser → static HTML | All content is build-time static. Mailto link uses siteConfig.email from FooterCTA. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | mailto link via FooterCTA | accept | Email intentionally public for lead gen; already used in Phase 2 FooterCTA pattern. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
Build passes:
  cd /Users/shannendorothee/Projects/Website && npm run build

Static pages emitted:
  /en/about, /id/about, /zh/about

BeliefGrid uses correct title size:
  grep "text-base font-bold" components/about/BeliefGrid.tsx

No Volt Green on about page:
  grep -v "volt-green" app/[locale]/about/page.tsx (should produce all lines — no volt-green references)
  grep "volt-green" components/about/BeliefGrid.tsx components/about/CurrentFocusParagraph.tsx
  Expected: no output (volt-green must not appear in about-specific files)
</verification>

<success_criteria>
npm run build exits 0; /[locale]/about resolves in all 3 locales; belief-statement H1 renders; BeliefGrid shows 3 columns with cyber-jade/20 accents; FooterCTA renders with cyber-jade outline button (no Volt Green).
</success_criteria>

<output>
Create .planning/phases/03-interior-pages/03-04-SUMMARY.md when done
</output>
