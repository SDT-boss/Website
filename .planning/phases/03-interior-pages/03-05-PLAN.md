---
phase: 03-interior-pages
plan: 05
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - messages/en.json
  - messages/id.json
  - messages/zh.json
  - components/solutions/SolutionDetailHero.tsx
  - components/solutions/CapabilityList.tsx
  - components/solutions/WhoThisIsFor.tsx
  - app/[locale]/solutions/[slug]/page.tsx
autonomous: true
requirements:
  - SOL-02
  - SOL-03
  - SOL-04

must_haves:
  truths:
    - "Visiting /en/solutions/fleet-mobility renders a 'Live' badge, H1, problem statement, WhoThisIsFor callout, and CapabilityList"
    - "Visiting /en/solutions/iot-hardware renders an 'In Development' badge plus the same content structure"
    - "All 4 × 3 = 12 slug+locale combinations resolve without 404 (generateStaticParams covers all)"
    - "dynamicParams = false is set on the [slug] page"
    - "Content order on every detail page: status badge → H1 → problem statement → WhoThisIsFor → CapabilityList"
    - "All 4 detail pages render correctly in ID and ZH locales"
  artifacts:
    - path: "components/solutions/SolutionDetailHero.tsx"
      provides: "Status badge + H1 + problem statement section"
      exports: ["SolutionDetailHero"]
    - path: "components/solutions/CapabilityList.tsx"
      provides: "Numbered capability items"
      exports: ["CapabilityList"]
    - path: "components/solutions/WhoThisIsFor.tsx"
      provides: "Left-bordered callout block"
      exports: ["WhoThisIsFor"]
    - path: "app/[locale]/solutions/[slug]/page.tsx"
      provides: "Solution detail route — 4 slugs × 3 locales"
      contains: "generateStaticParams"
  key_links:
    - from: "app/[locale]/solutions/[slug]/page.tsx"
      to: "lib/solutions.ts"
      via: "generateStaticParams + solution lookup"
      pattern: "import.*solutions.*from.*@/lib/solutions"
    - from: "app/[locale]/solutions/[slug]/page.tsx"
      to: "i18n/routing.ts"
      via: "routing.locales in generateStaticParams"
      pattern: "import.*routing.*from.*@/i18n/routing"
    - from: "components/solutions/SolutionDetailHero.tsx"
      to: "messages/en.json"
      via: "translations passed as props"
      pattern: "SolutionDetailHero.*status.*heading.*problem"
---

<objective>
Build solution detail pages as a complete vertical slice: add all 4 solution detail translation keys (problem statements, capabilities, who-this-is-for) to all 3 locale files, create SolutionDetailHero, CapabilityList, and WhoThisIsFor components, and implement app/[locale]/solutions/[slug]/page.tsx with generateStaticParams covering all 12 combinations.

Purpose: Every detail page (fleet-mobility, iot-hardware, logistics-intelligence, ai-operations) is reachable in all 3 locales with problem-first content and correct status badges.
Output: 3 new detail components, [slug]/page.tsx, 4 × solution detail namespaces added to all 3 locale files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-interior-pages/03-CONTEXT.md
@.planning/phases/03-interior-pages/03-RESEARCH.md
@.planning/phases/03-interior-pages/03-PATTERNS.md
@.planning/phases/03-interior-pages/03-UI-SPEC.md

<interfaces>
<!-- Key patterns extracted from codebase -->

From components/home/WorkTeaser.tsx (SolutionDetailHero analog — status badge, section layout):
  Section: <section className="py-24 md:py-32 px-6 bg-background">
  Container: <div className="mx-auto max-w-content">
  Live badge: <span className="self-start text-xs font-normal tracking-widest uppercase px-2 py-1 rounded bg-ev-teal/10 text-ev-teal">
  In Dev badge: <span className="self-start text-xs font-normal tracking-widest uppercase px-2 py-1 rounded bg-amber-500/10 text-amber-400">

From components/home/ApproachSection.tsx (CapabilityList analog — numbered items):
  const items = [{ num: "01", titleKey: "item1Title", bodyKey: "item1Body" }, ...]
  Number accent: <span className="text-4xl font-bold text-cyber-jade/20 leading-none">
  H3: className="text-base font-bold"  (NOT text-lg)
  Body: className="text-base font-normal leading-relaxed text-text-secondary"
  Layout: flex-col gap-8 (NOT grid — capabilities are vertical list, not 3-col grid)

WhoThisIsFor pattern (from RESEARCH.md Pattern 6, verified against tailwind.config.ts):
  <div className="border-l-2 border-cyber-jade pl-4 py-2">
    <p className="text-xs font-normal tracking-widest uppercase text-text-secondary mb-2">Who this is for</p>
    <p className="text-base font-normal leading-relaxed text-text-secondary">{text}</p>
  </div>

From app/[locale]/page.tsx + RESEARCH.md Pattern 1 (nested generateStaticParams):
  import { routing } from "@/i18n/routing"
  import { solutions } from "@/lib/solutions"
  export function generateStaticParams() {
    return routing.locales.flatMap((locale) => solutions.map((s) => ({ locale, slug: s.slug })))
  }
  export const dynamicParams = false
  async function Page({ params }: { params: Promise<{ locale: string; slug: string }> }) {
    const { locale, slug } = await params  // must await — Promise type in Next.js 14.2+
    setRequestLocale(locale)
    const solution = solutions.find((s) => s.slug === slug)
    if (!solution) notFound()
  }

From messages/en.json (solutions namespace — existing card keys stay; add slug sub-objects):
  The solutions sub-objects (fleet-mobility, iot-hardware, etc.) added in Plan 03-02 have: title, desc, tag
  Plan 03-05 adds: problemStatement, whoThisIsFor, capability1Title, capability1Body, capability2Title, capability2Body, capability3Title, capability3Body (per solution)

OPEN QUESTION (from RESEARCH.md A2): Validate getTranslations("solutions.fleet-mobility") syntax.
  If sub-namespace with hyphen doesn't work, use getTranslations("fleetMobility") as fallback with flat namespace.
  Resolution: Test in the page file. If build fails with sub-namespace, switch to flat namespaces per solution (e.g., "solutionFleetMobility").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add solution detail translation keys to all 3 locale files</name>
  <files>messages/en.json, messages/id.json, messages/zh.json</files>
  <read_first>
    - messages/en.json (read entire file — solutions slug sub-objects were added in Plan 03-02; now extend each with detail keys)
    - messages/id.json (read entire file)
    - messages/zh.json (read entire file)
    - .planning/PROJECT.md (company identity, domain — fleet/logistics/IoT/AI-ops)
    - .planning/phases/03-interior-pages/03-CONTEXT.md (D-01: Claude drafts copy; D-02: fleet-mobility = Live, others = In Development; D-03: outcome-first tone, no jargon, business operators audience)
    - DESIGN.md (voice rules: short, direct, no adjective bloat)
  </read_first>
  <action>
    Extend the slug sub-objects in the "solutions" namespace to include detail page keys. The sub-objects already have title/desc/tag from Plan 03-02. Add these additional keys to each slug sub-object in en.json:

    For "fleet-mobility":
    - "problemStatement": 2-3 sentence paragraph describing the operational problem this solution solves. Outcome-first, operator-credible. Example: "Fleet managers operating mixed EV and ICE vehicles struggle to unify dispatch, charging, and route planning in one view. Data lives in four different systems. Decisions get made on yesterday's information." Status is Live — the problem is solved in production.
    - "whoThisIsFor": 1-2 sentences describing the audience. Example: "Transport operators and fleet managers running 10 or more vehicles who need a single screen for dispatch, charging, and real-time vehicle state."
    - "capability1Title", "capability1Body": First capability (outcome-first). Example title: "Real-time fleet visibility". Body: "One dashboard shows every vehicle's state — location, charge level, and assignment — updated live. No polling required."
    - "capability2Title", "capability2Body": Second capability. Example: "Intelligent dispatch" — routes against live traffic and depot availability.
    - "capability3Title", "capability3Body": Third capability. Example: "Charging coordination" — schedules charging windows against shift patterns to reduce idle cost.

    For "iot-hardware":
    - "problemStatement": Industrial environments run on sensors and edge devices that most software ignores. Data sits on local machines or is too noisy to act on. The gap between field hardware and business decisions costs time and money.
    - "whoThisIsFor": Operations engineers and plant managers who need reliable data from the field without building custom integrations from scratch.
    - "capability1Title", "capability1Body": Edge device integration — connects to existing PLCs, sensors, and telemetry hardware without forklift replacement.
    - "capability2Title", "capability2Body": Signal processing — filters noise, detects anomalies, surfaces actionable events in real time.
    - "capability3Title", "capability3Body": Data pipeline to operations — sends clean, structured data upstream to dashboards and decision systems.

    For "logistics-intelligence":
    - "problemStatement": Ground operations lose efficiency to static schedules that don't respond to live conditions — traffic, load changes, and vehicle availability. Each replan is manual and takes time the operation doesn't have.
    - "whoThisIsFor": Logistics managers and operations planners who run multi-stop ground routes with variable loads and tight delivery windows.
    - "capability1Title", "capability1Body": Dynamic routing — adjusts routes in real time against traffic, capacity, and priority signals.
    - "capability2Title", "capability2Body": Load optimization — matches cargo weight and volume to vehicle capacity to reduce trip count.
    - "capability3Title", "capability3Body": Schedule adherence tracking — flags deviations from planned time windows before they compound.

    For "ai-operations":
    - "problemStatement": Operations generate enough data to act on — but the signal is buried in noise. Anomalies go undetected until they become incidents. Decisions that could be automated still require human review.
    - "whoThisIsFor": Operations leads and data teams who want to close the loop between sensor data and operational decisions without building ML pipelines from scratch.
    - "capability1Title", "capability1Body": Anomaly detection — flags deviations from expected behavior in real time across any data stream.
    - "capability2Title", "capability2Body": Predictive maintenance signals — surfaces early warnings from equipment data before failures affect operations.
    - "capability3Title", "capability3Body": Decision automation — defines rules and thresholds that trigger automated responses, reducing manual escalation.

    Per D-03: every capability body must describe an OUTCOME, not a technical feature. "Reduces idle time by routing against live traffic" not "Advanced routing algorithm."
    Per D-02: fleet-mobility status = "live"; others = "in-development" (handled by lib/solutions.ts, not translation keys).

    For id.json and zh.json: translate all new keys (problemStatement, whoThisIsFor, capability titles and bodies) for all 4 slugs following machine-translation pattern.
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/Website && node -e "const e=require('./messages/en.json');const fm=e.solutions['fleet-mobility'];console.log('problemStatement:',!!fm.problemStatement,'whoThisIsFor:',!!fm.whoThisIsFor,'cap1Title:',!!fm.capability1Title,'cap1Body:',!!fm.capability1Body);"</automated>
  </verify>
  <acceptance_criteria>
    - messages/en.json solutions["fleet-mobility"] contains "problemStatement", "whoThisIsFor", "capability1Title", "capability1Body", "capability2Title", "capability2Body", "capability3Title", "capability3Body" — all non-empty strings
    - Same 8 keys present on all 4 slug sub-objects: fleet-mobility, iot-hardware, logistics-intelligence, ai-operations
    - Existing keys (title, desc, tag) still present on each slug sub-object (not removed)
    - Existing card1Title, card2Title, card3Title, card4Title keys at solutions root still present
    - messages/id.json and messages/zh.json have matching structure for all 4 slugs
    - All 3 files remain valid JSON
  </acceptance_criteria>
  <done>All 4 solution detail namespaces populated in all 3 locale files; JSON valid; existing keys untouched</done>
</task>

<task type="auto">
  <name>Task 2: Create SolutionDetailHero, CapabilityList, WhoThisIsFor components and [slug]/page.tsx</name>
  <files>
    components/solutions/SolutionDetailHero.tsx,
    components/solutions/CapabilityList.tsx,
    components/solutions/WhoThisIsFor.tsx,
    app/[locale]/solutions/[slug]/page.tsx
  </files>
  <read_first>
    - components/home/WorkTeaser.tsx (SolutionDetailHero analog — status badge, section/container pattern)
    - components/home/ApproachSection.tsx (CapabilityList analog — numbered items, flex-col layout)
    - lib/solutions.ts (Solution type, solutions array, slug and status fields — created in Plan 03-01)
    - i18n/routing.ts (routing.locales for generateStaticParams)
    - app/[locale]/page.tsx (page setup pattern — await params, setRequestLocale)
    - messages/en.json (confirm detail keys from Task 1 exist before referencing)
    - .planning/phases/03-interior-pages/03-UI-SPEC.md (detail page layout: badge → H1 → problem → WhoThisIsFor → CapabilityList)
    - components/home/FooterCTA.tsx (reused at bottom of detail pages)
  </read_first>
  <action>
    Create 4 new files (3 components + 1 page). All in the components/solutions/ directory (already created by Plan 03-02's SolutionsGrid.tsx).

    FILE 1: components/solutions/SolutionDetailHero.tsx
    - Named synchronous component: export function SolutionDetailHero({ status, heading, problemStatement }: SolutionDetailHeroProps)
    - Interface: { status: "live" | "in-development"; heading: string; problemStatement: string }
    - Section wrapper: className="py-24 md:py-32 px-6 bg-background"
    - Container: className="mx-auto max-w-content flex flex-col gap-6"
    - Status badge (conditional on status value):
      If status === "live": <span className="self-start text-xs font-normal tracking-widest uppercase px-2 py-1 rounded bg-ev-teal/10 text-ev-teal">
        Content: the translated "Live" string. This component is synchronous and receives props — caller passes the translated status label.
        Use a prop for the label: add statusLabel: string to the interface.
      If status === "in-development": <span className="self-start text-xs font-normal tracking-widest uppercase px-2 py-1 rounded bg-amber-500/10 text-amber-400">
        Same pattern, different classes.
      Implementation: conditional className, not two separate elements — use a ternary for className.
      Actually simpler: receive a statusLabel prop (translated text) and a status prop for the className decision.
    - H1: <h1 className="text-5xl md:text-6xl font-bold tracking-tighter leading-none">{heading}</h1>
    - Problem statement: <p className="text-base font-normal leading-relaxed text-text-secondary">{problemStatement}</p>

    FILE 2: components/solutions/CapabilityList.tsx
    - Named synchronous component: export function CapabilityList({ capabilities }: { capabilities: Capability[] })
    - Interface Capability: { num: string; title: string; body: string }
    - Section wrapper: className="py-24 md:py-32 px-6 bg-background"
    - Container: className="mx-auto max-w-content"
    - Optional section label above the list: no — keep it clean, just the numbered items
    - List: className="flex flex-col gap-8"
    - Each item: <div key={cap.num} className="flex flex-col gap-3">
    - Number accent: <span className="text-4xl font-bold text-cyber-jade/20 leading-none">{cap.num}</span>
    - Title: <h3 className="text-base font-bold">{cap.title}</h3>
    - Body: <p className="text-base font-normal leading-relaxed text-text-secondary">{cap.body}</p>

    FILE 3: components/solutions/WhoThisIsFor.tsx
    - Named synchronous component: export function WhoThisIsFor({ text }: { text: string })
    - Section wrapper: className="py-12 md:py-16 px-6 bg-background"  (shorter padding — this is a callout, not a full section)
    - Container: className="mx-auto max-w-content"
    - Callout block: className="border-l-2 border-cyber-jade pl-4 py-2"
    - Label: <p className="text-xs font-normal tracking-widest uppercase text-text-secondary mb-2">Who this is for</p>
    - Body: <p className="text-base font-normal leading-relaxed text-text-secondary">{text}</p>

    FILE 4: app/[locale]/solutions/[slug]/page.tsx
    - Create the app/[locale]/solutions/[slug]/ directory
    - Imports: setRequestLocale, getTranslations from "next-intl/server"; notFound from "next/navigation"; solutions from "@/lib/solutions"; routing from "@/i18n/routing"; SolutionDetailHero from "@/components/solutions/SolutionDetailHero"; WhoThisIsFor from "@/components/solutions/WhoThisIsFor"; CapabilityList from "@/components/solutions/CapabilityList"; FooterCTA from "@/components/home/FooterCTA"
    - export function generateStaticParams() returning routing.locales.flatMap(locale => solutions.map(s => ({ locale, slug: s.slug })))
      This produces 12 combinations: 3 locales × 4 slugs
    - export const dynamicParams = false
    - Default async export: export default async function SolutionDetailPage({ params }: { params: Promise<{ locale: string; slug: string }> })
    - const { locale, slug } = await params  (must await — Promise type)
    - setRequestLocale(locale)
    - const solution = solutions.find(s => s.slug === slug)
    - if (!solution) notFound()
    - Validate next-intl sub-namespace: use const t = await getTranslations(`solutions.${slug}`)
      If build fails with this syntax, switch to a flat approach: create a helper that maps slug to a namespace key (e.g., "fleet-mobility" -> "fleetMobility") and use that as the namespace. Document the decision in the SUMMARY.
    - const ct = await getTranslations("common")
    - Build the capabilities array from translation keys:
      const capabilities = [
        { num: "01", title: t("capability1Title"), body: t("capability1Body") },
        { num: "02", title: t("capability2Title"), body: t("capability2Body") },
        { num: "03", title: t("capability3Title"), body: t("capability3Body") },
      ]
    - Determine the status label string:
      const statusLabel = solution.status === "live" ? ct("status.live") : ct("status.inDevelopment")
    - Return JSX in this content order (SOL-04 locked):
      1. SolutionDetailHero status={solution.status} statusLabel={statusLabel} heading={t("title")} problemStatement={t("problemStatement")}
      2. WhoThisIsFor text={t("whoThisIsFor")}
      3. CapabilityList capabilities={capabilities}
      4. FooterCTA
  </action>
  <verify>
    <automated>cd /Users/shannendorothee/Projects/Website && npm run build 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - components/solutions/SolutionDetailHero.tsx exists with named export SolutionDetailHero
    - SolutionDetailHero renders "bg-ev-teal/10 text-ev-teal" badge when status === "live"
    - SolutionDetailHero renders "bg-amber-500/10 text-amber-400" badge when status === "in-development"
    - SolutionDetailHero H1 uses "text-5xl md:text-6xl font-bold tracking-tighter leading-none"
    - components/solutions/CapabilityList.tsx exists with named export CapabilityList
    - CapabilityList H3 titles use "text-base font-bold"
    - components/solutions/WhoThisIsFor.tsx exists with named export WhoThisIsFor
    - WhoThisIsFor wrapper uses "border-l-2 border-cyber-jade"
    - app/[locale]/solutions/[slug]/page.tsx exists with default export SolutionDetailPage
    - page.tsx contains generateStaticParams using routing.locales.flatMap with solutions array
    - page.tsx contains dynamicParams = false
    - page.tsx awaits params (not destructures directly)
    - page.tsx calls setRequestLocale(locale) before getTranslations
    - page.tsx calls notFound() when solution is not found
    - Content order in JSX: SolutionDetailHero → WhoThisIsFor → CapabilityList → FooterCTA
    - npm run build exits 0 and emits all 12 slug×locale combinations:
      /en/solutions/fleet-mobility, /en/solutions/iot-hardware, /en/solutions/logistics-intelligence, /en/solutions/ai-operations
      /id/solutions/fleet-mobility, /id/solutions/iot-hardware, etc.
      /zh/solutions/fleet-mobility, /zh/solutions/iot-hardware, etc.
  </acceptance_criteria>
  <done>All 4 solution detail pages build in all 3 locales; content order correct; status badges correct; 12 static pages emitted</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL slug → solution lookup | slug from URL params is validated against solutions array; notFound() called if no match. No SQL injection risk — comparison is against a static TypeScript array. |
| Build-time → static HTML | generateStaticParams generates all valid slug×locale paths at build time; dynamicParams=false rejects unknown slugs with 404 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Spoofing | Invalid slug in URL | mitigate | solutions.find() returns undefined; notFound() is called, rendering the existing not-found.tsx page. dynamicParams=false prevents any unregistered slug from being served. |
| T-03-06 | Information Disclosure | Static detail page content | accept | Content is intentionally public marketing copy. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
Build passes and all 12 detail pages emitted:
  cd /Users/shannendorothee/Projects/Website && npm run build

Check all 12 paths in build output — look for entries like:
  ● /en/solutions/fleet-mobility
  ● /id/solutions/fleet-mobility
  ● /zh/solutions/fleet-mobility
  (4 slugs × 3 locales = 12 total)

generateStaticParams coverage:
  grep "flatMap" app/\[locale\]/solutions/\[slug\]/page.tsx

dynamicParams is false:
  grep "dynamicParams" app/\[locale\]/solutions/\[slug\]/page.tsx

notFound guard present:
  grep "notFound" app/\[locale\]/solutions/\[slug\]/page.tsx

WhoThisIsFor before CapabilityList (SOL-04):
  grep -n "WhoThisIsFor\|CapabilityList" app/\[locale\]/solutions/\[slug\]/page.tsx
  WhoThisIsFor line number must be smaller than CapabilityList line number
</verification>

<success_criteria>
npm run build exits 0; all 12 solution detail pages emitted as static HTML; status badges correct (fleet-mobility = ev-teal Live, others = amber In Development); content order is badge → H1 → problem → WhoThisIsFor → CapabilityList → FooterCTA.
</success_criteria>

<output>
Create .planning/phases/03-interior-pages/03-05-SUMMARY.md when done
</output>
